# CHANGELOG — Evolution Log

Every build prompt processed by the orchestrator becomes the next numbered
entry. Maintained by review-agent (format below); numbers are contiguous.

---

## #23 — 2026-06-15 — In-player controls layer (Fullscreen + PiP toggles + keyboard shortcuts) over the existing dual-engine `<video>`, plus a single persisted client-wide volume/mute preference (ADR-0039/0040)

- **Prompt:** Add an in-player controls layer over the existing dual-engine `<video>` player — a Fullscreen toggle (native Fullscreen API), a Picture-in-Picture toggle (native `requestPictureInPicture`, feature-detected and hidden where unsupported), and keyboard shortcuts active only while a stream is playing (F = fullscreen, P = PiP, Space/K = play-pause, M = mute, ArrowUp/ArrowDown = volume) — surfaced as accessible, keyboard-focusable controls on the player chrome alongside the format chip, driving the already-resolved `<video>` directly and applying uniformly to live, catch-up, and VOD; degrade silently where Fullscreen/PiP is unavailable; no new server route, no new playback engine, no new state-machine phase, no new playback localStorage key (one client-wide volume/mute preference may persist, mirroring theme/sort); demo mode must demonstrate the controls offline. (The E22 Phase-4 research winner.)
- **Outcome:** Shipped. All 5 tasks done and validated on the **first attempt each**. A purely **additive client-only control layer** over the existing shared `#player-video` — **no** new server route (`src/server/**` unchanged), **no** new playback engine, **no** new state-machine phase, and exactly **one** new localStorage key (`iptv_vol`, the single allowed volume/mute preference, ADR-0040). A new self-contained module `src/client/ctrl.js` (`window.IptvCtrl`) exposes the control primitives that act on the resolved `<video>`: pure feature predicates `hasFs()` / `hasPip()` (driving the silent-degrade hide), the `toggleFs()` / `togglePip()` toggles (prefixed fallbacks, rejection swallowed, PiP no-op where unsupported), the media actions `togglePlay()` / `toggleMute()` / `volUp()` / `volDn()` (stepping `ST.vol` by `S.volStp` clamped to `[0,1]`, persisting via `IptvSt.setVol` / `setMuted`), and the `mkCtrl` state-sync subscriptions to `fullscreenchange` (+ webkit) and `enter/leavepictureinpicture` so the buttons' `aria-pressed` follows the **actual** browser state (an Escape-driven fullscreen exit un-presses the button), never an optimistic guess — all native APIs accessed through `window`/`document`/the element so units can mock them. The controls chrome adds two keyboard-focusable buttons `#fs-btn` / `#pip-btn` in the content-head cluster alongside the format chip (`aria-pressed="false"` **present in the baseline**, value-only mutation per R-0001), `EL.fsb` / `EL.pipb` declared once in the `EL` literal and set in `mkEL`, `rndCtrls()` applying feature-detect hide + state sync, and `onFsBtn` / `onPipBtn` click handlers wired through the same `mkEL` path as the other content-head controls. The keyboard layer is a **separate** document `keydown` handler `onPlayKey` (with the pure `getPlayAct` map) that returns immediately unless `IptvSt.ST.phase === 'PLAY'` and the target is non-typing (input/textarea/select/contentEditable), maps F→`toggleFs`, P→`togglePip`, Space/K→`togglePlay`, M→`toggleMute`, ArrowUp→`volUp`, ArrowDown→`volDn`, `preventDefault`s only the keys it consumes, and **never** handles Escape — `onAcctKey` (ADR-0014/0028) stays the sole Escape/panel-close owner, untouched. `main.js` initialises `IptvCtrl` after the `<video>` reference is established, applies the persisted `iptv_vol` preference, and calls `rndCtrls()` before any connect flow. ADR↔code traceability holds for ADR-0039/0040; the run carries one persistent recovered near-miss (TASK-0085 omitted the `ctrl.js` script include; TASK-0086 added it — see Notes).
- **ADRs:** ADR-0039 (`docs/adrs/ADR-0039-in-player-controls-layer.md`, **accepted**) — a client-only in-player controls layer (Fullscreen + PiP toggles + keyboard shortcuts) over the shared `<video>`: the new `src/client/ctrl.js` (`window.IptvCtrl`) control engine, the `#fs-btn` / `#pip-btn` chrome + `EL.fsb` / `EL.pipb` + `rndCtrls` + click handlers in `ui.js`, the `onPlayKey` keyboard handler (active only in PLAY, non-typing, never Escape), init in `main.js`, styling in `app.css`, and the demo recording; no server route, no engine, no phase. `governs:` declares `src/index.html`, `src/client/{ctrl,ui,main}.js`, `src/client/app.css`, `src/tests/unit/ctrl.test.js`, `src/tests/ui/{ctrl,ctrl-demo}.test.js`. ADR-0040 (`docs/adrs/ADR-0040-volume-mute-persistence.md`, **accepted**) — persist a **single client-wide** volume/mute preference under one new localStorage key `iptv_vol` (`S.volKey` in `cfg.js`; `loadVol`/`saveVol` validate-and-default readers/writers in `st.js`; the existing `setVol`/`setMuted` persist on write), following the theme/sort chrome-persistence pattern (ADR-0003), not part of the phase machine. `governs:` declares `src/client/cfg.js`, `src/client/st.js`, `src/tests/unit/vol.test.js`. Both ADRs are first-in-force this evolution; no prior ADR is contradicted, superseded, or deleted (ADR-0025 keeps owning the format chip — the controls are siblings, not a replacement).
- **Tasks:** 5 done, 0 failed, 0 blocked. TASK-0084 — `S.volKey = 'iptv_vol'` in cfg.js; `loadVol`/`saveVol` in st.js with validation + defaults; `setVol`/`setMuted` persist on write — commit `dcd2abb`, first-attempt · TASK-0085 — control module `src/client/ctrl.js` (`window.IptvCtrl`): feature detection + toggles + media actions + state-sync subscriptions — commit `ecb693d`, first-attempt · TASK-0086 — controls chrome: `#fs-btn` / `#pip-btn` markup (aria-pressed baseline), `EL.fsb` / `EL.pipb`, `rndCtrls`, click handlers + wiring, init in main.js, CSS (and the `ctrl.js` script include) — commit `aae0021`, first-attempt · TASK-0087 — keyboard shortcuts (`onPlayKey` document handler, active only in PLAY, non-typing target, no Escape collision) — commit `e01bfe7`, first-attempt · TASK-0088 — demo recording of the in-player controls layer — commit `80c129b`, first-attempt.
- **Tests:** 1026 unit (Vitest, 42 files) — all green, including the E23-specific `src/tests/unit/vol.test.js` (30: `S.volKey` is `iptv_vol` + frozen-S, `loadVol` round-trip / boundary 0+1 / absent / malformed JSON / non-object / null / out-of-range / non-number / non-finite / non-boolean-muted / getItem-throws fallbacks, `saveVol` round-trip + setItem-throws guard, `setVol`/`setMuted` mutate+persist+round-trip, `ST.phase` unaffected) and `src/tests/unit/ctrl.test.js` (81: `hasFs`/`hasPip` true/false incl. iOS-Safari no-PiP, the toggles' request/exit/no-op/promise-swallow, the media actions, volume stepping + clamping at 0 and 1 with persistence, the `mkCtrl` state-sync subscriptions, the TASK-0086 chrome wiring — `mkEL` click wiring, the R-0001 baseline-present `aria-pressed` guard, feature-detect hide, `aria-pressed`/`is-on` value sync, `onFsBtn`/`onPipBtn` routing — and the TASK-0087 `onPlayKey` cases: the key→action map in PLAY on a non-typing target, the active-only-while-playing guard across every non-PLAY phase, the typing-context guard, `preventDefault` only on a consumed key, and Escape collision-safety). UI (Playwright): 292 product UI tests pass, including `src/tests/ui/ctrl.test.js` (6: both controls render with baseline `aria-pressed`, fullscreen button keyboard-focusable, click routing, state-sync un-press on exit, unsupported PiP hidden + not tab-reachable, screenshot), `src/tests/ui/keys.test.js` (6: `M` mute toggle on the live `<video>`, ArrowUp/Down volume, Space play/pause, typing `M` in search does NOT hijack, no-op when not playing, Escape still closes the account panel while playing), and `src/tests/ui/ctrl-demo.test.js` (8: the recorded arc, all driving the genuine production handlers). **Environment-red note (no live network):** the only reds are the **documented pre-existing no-network environment-red set** (CHANGELOG #17): `src/tests/ui/log-demo.test.js` / `src/tests/ui/fmtchip-demo.test.js` (`expected "HLS not supported", received "levelLoadError"` — no live CDN) + downstream serial steps, `src/tests/ui/live.test.js` (live portal unreachable), and the live-network integration tier (15 passed / 16 environment-red, every failure rooted in unreachable live endpoints). These are **not** E23 regressions: the run adds no `src/server/**`/proxy code and no new external connectivity (the controls are client-only over the existing player). Verified green locally at review: `npx vitest run` → 1026 passed (42 files).
- **Demo:** `test-results/e23-controls-demo.webm` (~578 KB, committed on the run branch via TASK-0088), referenced from PR #47's `### Demo` as a clickable file-viewer (blob) link (private repo). Recorded arc: **boot** (fresh app load via the Playwright `webServer`, controls chrome wired and idle) → **prepare** (footer demo connect + play a demo channel so the shared `<video>` is the play target and the controls are active) → **interact** (show + activate the Fullscreen `#fs-btn` / PiP `#pip-btn` buttons — degrading silently for real FS/PiP in headless Chromium — and drive the genuine document `onPlayKey` handler: M mute, ArrowUp/ArrowDown volume, Space play/pause on the real `<video>`) → **revert runtime state** (in-app: un-mute, restore default volume, exit any lingering FS/PiP, stop the demo stream, return to a non-PLAY idle condition) → **stop**. E23 adds user-interactable behaviour, so it is **not** demo-exempt; the `### Demo` reference is present and accurate (confirmed at review).
- **Rules earned:** none. The run surfaces **one persistent recovered near-miss** (root-cause-tag `missing-script-include`, first occurrence — below the §5 recurrence threshold of 2, so no rule yet): TASK-0085 created `src/client/ctrl.js` but omitted its `<script src="/ctrl.js">` include in `src/index.html`, so `window.IptvCtrl` would not have existed at runtime; TASK-0086 added the include (caught + fixed within the run). The 3 pre-existing unit harnesses (`foot`/`m3u-ui`/`persist`) that gained a one-line `addEventListener` stub for harness completeness in TASK-0087 are not a defect — no near-miss.
- **Artifacts:** `src/client/ctrl.js` (new control engine, `// ADR: ADR-0039`), `src/client/ui.js` (`EL.fsb`/`EL.pipb` + `rndCtrls` + `onFsBtn`/`onPipBtn` + `getPlayAct`/`onPlayKey` keyboard handler, `ADR-0039`), `src/client/main.js` (`IptvCtrl` init + apply persisted `iptv_vol` + `rndCtrls`, `ADR-0039`/`ADR-0040`), `src/client/app.css` (`#fs-btn`/`#pip-btn` content-head control rules, `ADR-0039`), `src/index.html` (`#fs-btn`/`#pip-btn` markup + `ctrl.js` script include, `<!-- ADR: ADR-0039 -->`), `src/client/cfg.js` (`S.volKey = 'iptv_vol'`, `ADR-0040`), `src/client/st.js` (`loadVol`/`saveVol` + persist-on-write `setVol`/`setMuted`, `ADR-0040`), `docs/adrs/ADR-003{9}-*.md` + `docs/adrs/ADR-0040-*.md`, `tasks/TASK-008{4,5,6,7,8}-*.md`, `src/tests/unit/{vol,ctrl}.test.js`, `src/tests/ui/{ctrl,keys,ctrl-demo}.test.js`, `test-results/e23-controls-demo.webm`, `test-results/task-0086-controls-chrome.png`, `README.md` (intro in-player-controls paragraph + new in-player controls layer feature bullet). Traceability: all 8 of ADR-0039's `governs:` paths and all 3 of ADR-0040's resolve to tracked files at the branch tip, each carrying its `ADR-0039`/`ADR-0040` reference comment (`main.js` carries both); no E23 code references a deleted ADR; both ADRs stay `accepted` with non-empty `governs:`.
- **Notes:** branch `ai/e23-player-controls-layer`, PR **#47**, **stacked on the unmerged E22 branch** (`ai/e22-vod-movies-series`, PR #45) → **E21** (`ai/e21-catchup-archive-replay`, PR #43) → **E20** (`ai/e20-epg-program-reminders`, PR #41) → **E19** (`ai/e19-epg-now-next-guide`, PR #39) → **E18** (`ai/e18-product-memory-file`, PR #37) — cut from E22's tip, not fresh `main`, so PR #47's diff against `main` carries the E22+E21+E20+E19+E18 commits and **depends on those PRs merging first (or together, oldest first)**, mirroring prior stacked-run entries. validate-agent wrote every per-task Test Results block into PR #47 and this REVIEW finalized it; the repo is private, so the demo + controls-chrome screenshot are clickable blob-viewer links. **One persistent recovered near-miss routed to the orchestrator (review-agent does not write `failures/`):** root-cause-tag `missing-script-include` — TASK-0085 created `ctrl.js` without its `index.html` `<script>` include (so `window.IptvCtrl` would be undefined at runtime), fixed by TASK-0086 adding the include; first occurrence, below the §5 recurrence threshold, so no rule yet. No status-fix open items: every E23 task records `attempts: 1` for its clean first-attempt pass (R-0002 satisfied).

---

## #22 — 2026-06-15 — VOD (Movies & Series) library on the Xtream path: normalize VOD movies/series into a parallel on-demand `Vod` shape fetched best-effort through the existing proxy, presented behind a Live | Movies | Series content toggle that reuses the sidebar/grid and the existing select+play path (ADR-0037/0038)

- **Prompt:** Add a VOD (Movies & Series) library on the Xtream path — after connect, fetch VOD movie categories/streams (`get_vod_categories` / `get_vod_streams`) and TV series (`get_series`, then `get_series_info` for seasons/episodes) best-effort through the existing `/api/xtream` CORS proxy, normalize each into a canonical on-demand item schema mirroring `mkXtCh`, and present them in the existing sidebar/category + channel-grid browse UX behind a Live | Movies | Series content toggle (posters/title from the VOD payload); selecting a movie or a series episode plays it through the existing dual-engine player and select+play path a live channel uses, building the Xtream on-demand stream URL (`/movie/…` for movies, `/series/…` for episodes, extension preserved so `getEng` resolves the same engine); M3U/demo show no VOD surface and degrade silently (the demo path may synthesize one offline-playable VOD entry); no new server route, no new playback engine, no new state-machine phase, no new localStorage key for playback. (The E21 Phase-4 research winner.)
- **Outcome:** Shipped. All 7 tasks done and validated on the **first attempt each**. A purely **additive parallel-data + render-mode + URL-building** layer on the existing Xtream fetch/browse/play machinery — no new server route, no new playback engine, no new state-machine phase, no new localStorage key, and **no change to the live `Ch` type**. A new self-contained client module `src/client/vod.js` (`window.IptvVod`) holds a session-scoped, Map-backed VOD store plus the **pure** normalizers that map raw `get_vod_streams` / `get_series` / `get_series_info` payloads into the **parallel** on-demand `Vod` (`{id,name,grp,url,img,cat,num,kind:'movie'|'episode'}`) and `Series` (`{id,name,grp,img,cat}`) shapes, and the **pure** `/movie/…` and `/series/…` on-demand URL builders (container extension preserved so `getEng` resolves the **same** engine with no new `loadPlay` branch). After a successful Xtream connect a best-effort, non-blocking VOD pass in `src/client/api.js` (mirroring the EPG fetch posture, ADR-0030) fetches movies + series through the **existing** proxy/`loadJson` (15 s `AbortController` timeout reused), normalizes them, and writes `window.IptvVod`; series seasons/episodes are fetched **on demand** per series; failure/empty/timeout is swallowed (empty VOD store, no Movies/Series tabs), and M3U/demo leave the store empty except the demo path's single **synthesized offline-playable movie** (public HLS test stream). A keyboard-accessible **Live | Movies | Series** segmented toggle (rendered above the grid) switches a **transient render-mode flag** in the existing UI/state layer (not a phase, no localStorage key — resets to `live` on connect/switch/disconnect/reload) and re-renders the **reused** `rndSide`/`rndGrid` for that mode's categories/items; the Movies/Series options are shown **only when** `window.IptvVod` actually has them (contextual presence). Selecting a movie card or a series episode drives the **existing** select+play arc — `setCur` → `saveSt('sel')` → `go('PLAY')` when `READY` → `rndHead()` → `IptvPlay.loadPlay(item.url)` — exactly like a live card click / `goRemWatch` / `goReplay`. ADR↔code traceability holds for ADR-0037/0038; the run carries one task-file `attempts` correction routed to the orchestrator (see Notes).
- **ADRs:** ADR-0037 (`docs/adrs/ADR-0037-vod-data-model-and-fetch.md`, **accepted**) — normalize Xtream VOD movies/series into a parallel on-demand `Vod` item shape (mirroring the render/play-relevant `Ch` fields plus a `kind` discriminator) and a browse-only `Series` shape, fetched best-effort/non-blocking through the existing proxy into a dedicated `window.IptvVod` store, with pure extension-preserving `/movie/…` and `/series/…` URL builders; the live `Ch` type is unchanged; no new server route, no new engine, no new state phase, no new localStorage key; demo synthesizes one offline-playable movie. `governs:` declares `src/client/{api,vod}.js`, `src/tests/unit/{api,vod,vodfetch}.test.js`, `src/tests/int/vod.test.js`. ADR-0038 (`docs/adrs/ADR-0038-content-toggle-and-vod-playback.md`, **accepted**) — present VOD behind a Live | Movies | Series content toggle (a transient render-mode flag, not a phase) re-rendering the reused sidebar/grid per mode with contextual Movies/Series presence, a series seasons/episodes drill-down (on-demand `get_series_info`, episodes grouped by season, back affordance), and on-demand select+play wiring that reuses the existing arc; R-0001 compliance (toggle aria attributes baseline-present, only flipped; one-shot controls carry no toggled attribute). `governs:` declares `src/client/ui.js`, `src/client/app.css`, `src/index.html`, `src/tests/unit/vodui.test.js`, `src/tests/ui/{vod,vod-demo,vod-recording}.test.js`. Both ADRs are first-in-force this evolution; no prior ADR is contradicted, superseded, or deleted.
- **Tasks:** 7 done, 0 failed, 0 blocked. TASK-0077 — `Vod`/`Series` normalizers, on-demand URL builders, and the `window.IptvVod` store module (`vod.js`, pure, uniquely-named flat-scope bindings) — commit `9c6149d`, first-attempt · TASK-0078 — best-effort VOD/series fetch wired after Xtream connect (movies + series + on-demand series-info) populating `window.IptvVod`, demo synthesizes one offline movie — commit `085b678`, first-attempt · TASK-0079 — Live | Movies | Series content toggle (segmented control, content-mode flag, contextual presence) re-rendering the sidebar/grid per mode — commit `d8a3df2`, first-attempt · TASK-0080 — Movies browse (VOD movie categories in the sidebar + movie poster cards in the grid, reusing `rndSide`/`rndGrid`/`mkCard`) — commit `28a84f5`, first-attempt · TASK-0081 — series browse + seasons/episodes drill-down (series cards, on-demand `get_series_info`, episode entries grouped by season, back affordance) — commit `fd2c4b2`, first-attempt · TASK-0082 — on-demand select+play wiring (movie/episode plays via the existing select+play path; engine resolves from the preserved extension) — commit `a64af12`, first-attempt · TASK-0083 — demo recording of VOD browse + play — commit `321f078`, first-attempt.
- **Tests:** 915 unit (Vitest, 40 files) — all green, including the E22-specific `src/tests/unit/vod.test.js` (~24: `Vod`/`Series` normalizers, the pure `/movie/…` / `/series/…` URL builders with `container_extension` preservation + `getExt` fallback, store set/get-copy/clear, per-series episode flattening), `src/tests/unit/vodfetch.test.js` (~16: best-effort movies + series fetch through the proxy, on-demand `get_series_info`, swallow-on-reject/empty/timeout, never-throws, demo synthesized movie, empty store on M3U/demo), and `src/tests/unit/vodui.test.js` (~42: content toggle render + contextual Movies/Series presence + baseline-present aria (R-0001) + mode switch re-render + reset-to-All, movies/series sidebar+grid render via the reused functions, series drill-down open → episodes-by-season → back, on-demand select+play reusing the live arc, one-shot controls carry no toggled attribute). UI (Playwright): 271 product UI tests pass, including `src/tests/ui/vod.test.js` (toggle present/keyboard-accessible, switch to Movies renders movie cards, series drill-down open → episodes → back, episode/movie play through the normal player) and `src/tests/ui/vod-demo.test.js` / `src/tests/ui/vod-recording.test.js` (the recorded arc). **Environment-red note (no live network):** the only reds are the **documented pre-existing no-network environment-red set** (CHANGELOG #17): `src/tests/ui/log-demo.test.js` (`expected "HLS not supported", received "levelLoadError"` — no live CDN) + downstream serial steps, `src/tests/ui/fmtchip-demo.test.js`, `src/tests/ui/live.test.js` (live portal unreachable), and the live-network integration tier including the new `src/tests/int/vod.test.js` (VOD movies/series shape over the live portal). These are **not** E22 regressions: the run adds no `src/server/**` or proxy code and no new external connectivity (VOD fetch + on-demand play reuse the existing `/api/xtream` proxy and dual-engine path). Integration: when outbound network was available mid-run the full int tier ran green; on the no-network passes the live tier is environment-red (transient sandbox condition, not a defect). Verified green locally at review: `npx vitest run` → 915 passed (40 files).
- **Demo:** `test-results/e22-vod-demo.webm` (~387 KB, committed on the run branch via TASK-0083), referenced from PR #45's `### Demo` as a clickable file-viewer (blob) link (private repo). Recorded arc: **boot** (fresh `http://localhost:3000` load, idle) → **prepare** (footer demo connect; the synthesized offline movie surfaces a Movies tab in the content toggle) → **interact** (switch the toggle to **Movies**, select & play the synthesized "Demo Movie" through the normal player — it plays via the existing engine path with no live portal) → **revert runtime state** (in-app: stop playback, switch the toggle back to **Live**) → **stop**. E22 adds user-interactable behaviour, so it is **not** demo-exempt; the `### Demo` reference is present and accurate (confirmed at review).
- **Rules earned:** none (no terminal failures; first-attempt pass each, no persistent recovered near-miss). The implementer proactively gave `vod.js`'s top-level bindings unique names to avoid the documented flat-scope shared-global collision — a prevention by design, not a recovered failure, so no near-miss is recorded.
- **Artifacts:** `src/client/vod.js` (new, pure store + normalizers + URL builders, `// ADR: ADR-0037`), `src/client/api.js` (best-effort VOD fetch after connect + demo movie synthesis, `ADR-0037`), `src/client/ui.js` (content toggle + per-mode sidebar/grid render + series drill-down + on-demand select+play, `ADR-0038`), `src/client/app.css` (content-toggle + VOD/series/drill-down rules, `ADR-0038`), `src/index.html` (`vod.js` script load order + toggle markup, `<!-- ADR: ADR-0038 -->`), `docs/specs/vod-library.md` (new feature spec), `docs/adrs/ADR-003{7,8}-*.md`, `tasks/TASK-007{7,8,9}-*.md` + `tasks/TASK-008{0,1,2,3}-*.md`, `src/tests/unit/{api,vod,vodfetch,vodui}.test.js`, `src/tests/int/vod.test.js`, `src/tests/ui/{vod,vod-demo,vod-recording}.test.js`, `test-results/e22-vod-demo.webm`, `README.md` (intro VOD paragraph + new VOD library (Movies & Series) feature bullet). Traceability: all 6 of ADR-0037's `governs:` paths and all 7 of ADR-0038's resolve to tracked files at the branch tip, each carrying its `ADR-0037`/`ADR-0038` reference comment; no E22 code references a deleted ADR; both ADRs stay `accepted` with non-empty `governs:`.
- **Notes:** branch `ai/e22-vod-movies-series`, PR **#45**, **stacked on the unmerged E21 branch** (`ai/e21-catchup-archive-replay`, PR #43) → **E20** (`ai/e20-epg-program-reminders`, PR #41) → **E19** (`ai/e19-epg-now-next-guide`, PR #39) → **E18** (`ai/e18-product-memory-file`, PR #37) — cut from E21's tip, not fresh `main`, so PR #45's diff against `main` carries the E21+E20+E19+E18 commits and **depends on those PRs merging first (or together, oldest first)**, mirroring prior stacked-run entries (E12–E15/E19/E20/E21). The non-deterministic `test-results/*.webm|*.png` byte-regenerations from the UI regression runs were deliberately left **unstaged** (out of scope, mirroring prior runs). validate-agent wrote every per-task Test Results block into PR #45 and this REVIEW finalized it; the repo is private, so the demo is a clickable blob-viewer link. **One open item routed to the orchestrator at finalization (review-agent does not edit task files):** **status-fix** — TASK-0080's task file carries `attempts: 0` for a clean first-attempt pass (every other E22 task records `attempts: 1`); reported as a `status-fix` discrepancy (identical to the E19 TASK-0064 and E20 TASK-0068 corrections). It affects no code or test correctness — both the deterministic unit (915/915) and UI (271) gates are green.

---

## #21 — 2026-06-15 — Catch-up (archive/timeshift) Replay on the EPG: detect per-channel Xtream archive support and offer a keyboard-focusable Replay on past schedule rows, playing the timeshift archive through the existing proxy + dual-engine player (ADR-0035/0036)

- **Prompt:** Add catch-up (archive/timeshift) playback on top of the EPG — detect per-channel archive support from the Xtream source (`tv_archive` / `tv_archive_duration`) via the existing fetch path, and on archive-capable channels render a keyboard-focusable Replay affordance on each past schedule row of the expandable per-channel guide; activating it builds the Xtream timeshift archive URL for that program (start + duration) and plays it through the existing CORS proxy and dual-engine player by reusing the existing select+play path; past rows on non-archive channels and the whole feature on M3U/demo show no affordance and degrade silently; no new playback engine, no new state-machine phase, no new server route. (The E20 Phase-4 research winner.)
- **Outcome:** Shipped. All 5 tasks done and validated on the **first attempt each**. A purely **additive data + render + URL-building** layer on the E19/E20 EPG — no new playback engine, no new state-machine phase, no new server route, no new localStorage key. The canonical `Ch` schema (CONVENTIONS §7) gained **two optional, safe-defaulted fields** — `arch:boolean` (default `false`) and `archDur:number` (retention days, default `0`) — populated in `mkXtCh` from the raw `get_live_streams` `tv_archive` / `tv_archive_duration` already fetched on connect, and defaulted on the M3U/demo paths, so every existing `Ch` consumer (grid filter, sort, favourites, live play) is unaffected. A **pure** `getArchUrl({ch,prg})` helper in `src/client/play.js` builds the Xtream timeshift URL (`<base>/timeshift/<user>/<pass>/<durMinutes>/<YYYY-MM-DD:HH-MM>/<id>.<ext>`) by mirroring the channel's live URL form — preserving its extension so `getEng` resolves the **same** engine — and returns a non-`/live/` url unchanged so the offline demo path plays its public HLS test stream as-is. `mkSchedRow` (`src/client/ui.js`) renders a keyboard-focusable `<button class="ch-replay">` carrying `data-replay="<chId>|<start>"` and a baseline-present `aria-label="Replay <title>"` (R-0001 — one-shot action, no attribute toggling) only when the program is **past** (`prg.stop <= now`), the channel is **archive-capable** (`arch===true`), and the start is within the `archDur`-day window (window check skipped when `archDur===0`); future rows still get Remind (ADR-0033), the airing program gets neither, non-archive/M3U/demo rows get nothing. `onGridClick` routes a `[data-replay]` click (with `stopPropagation`, before the card-select branch) to `goReplay(chId,start)`, which resolves the `Ch` (no-op if gone / not `arch`) and the `Prg` from `IptvEpg.get(chId)` (no-op if absent), builds the archive URL, and drives the **existing** select+play arc — `setCur` → `saveSt('sel')` → `go('PLAY')` when `READY` → `rndHead()` → `IptvPlay.loadPlay(archUrl)` — exactly like a live card click / `goRemWatch`. The demo EPG path synthesizes an `arch:true` demo channel with a past program so a Replay row renders offline and the recording can exercise it. ADR↔code traceability holds for ADR-0035/0036; the run carries **no open items**.
- **ADRs:** ADR-0035 (`docs/adrs/ADR-0035-archive-capability-on-ch.md`, **accepted**) — carry per-channel archive capability (`arch`/`archDur`) on the canonical `Ch` schema, populated from the Xtream `tv_archive` / `tv_archive_duration` via the existing connect fetch path; both fields optional with safe defaults so existing consumers are unaffected; M3U/demo default to `false`/`0`; no new store, endpoint, or data type. `governs:` declares `CONVENTIONS.md` (schema home, ADR-side-linked), `src/client/api.js`, `src/tests/unit/api.test.js`, `src/tests/int/catchup.test.js`. ADR-0036 (`docs/adrs/ADR-0036-replay-affordance-and-archive-playback.md`, **accepted**) — render a Replay affordance on past archive-capable schedule rows that builds the Xtream timeshift URL and reuses the existing select+play path (the past mirror of ADR-0033's Remind); the pure `getArchUrl` builder, the `mkSchedRow` Replay control, and the `goReplay` activation through `onGridClick`, plus demo synthesis for offline demonstrability; no new engine, phase, or route. `governs:` declares `src/client/{play,ui,api}.js`, `src/client/app.css`, `src/tests/unit/{play,epgui,api,epgfetch}.test.js`, and `src/tests/ui/{catchup,catchup-demo,catchup-recording}.test.js`. Both ADRs are first-in-force this evolution; no prior ADR is contradicted, superseded, or deleted.
- **Tasks:** 5 done, 0 failed, 0 blocked. TASK-0072 — optional `arch`/`archDur` on the `Ch` schema, populated in `mkXtCh` (truthy/numeric coercion), defaulted in `mkM3uCh`/`mkDemoCh` — commit `0bbf259`, first-attempt · TASK-0073 — pure `getArchUrl` + timeshift stamp/duration helpers in `play.js`, exported on `window.IptvPlay` — commit `189ee8f`, first-attempt · TASK-0074 — Replay control on past archive-capable rows in `mkSchedRow` (gating, `data-replay`, baseline `aria-label`, styling) — commit `ea3498a`, first-attempt · TASK-0075 — `goReplay` activation wired through `onGridClick` reusing the select+play path + demo EPG archive synthesis — commit `0414925`, first-attempt · TASK-0076 — demo recording of Replay on a past archive-capable program — commit `c099592`, first-attempt.
- **Tests:** 833 unit (Vitest, 37 files) — all green, including the E21-specific `Ch archive fields (arch/archDur)` cases in `src/tests/unit/api.test.js` (truthy/string/0/absent/NaN coercion in `mkXtCh`; every demo + M3U channel `arch:false`/`archDur:0`; demo synthesizes ≥1 archive-capable channel), the 16 `getArchUrl()` cases in `src/tests/unit/play.test.js` (TS + HLS URL build, base/user/pass/id/ext preservation, local `YYYY-MM-DD:HH-MM` stamp, whole-minute duration rounding + minimum 1, `getEng(archUrl) === getEng(ch.url)`, purity, `window.IptvPlay` export), the `Replay control gating` + `goReplay` cases in `src/tests/unit/epgui.test.js` (past+in-window renders the control with baseline `aria-label` (R-0001) + escaped title; future → Remind only; airing → neither; non-archive/no-`arch`/out-of-window → none; `archDur===0` skips the window check; `goReplay` builds the timeshift URL not the live url and runs the full READY arc, no-ops for unknown/non-archive/no-guide channels), and the demo past-program synthesis case in `src/tests/unit/epgfetch.test.js`. UI (Playwright): 244 product UI tests pass, including `src/tests/ui/catchup.test.js` (Replay present + focusable on the past archive row only, never the future row, absent on non-archive channels), `src/tests/ui/catchup-demo.test.js` (activating Replay plays the timeshift form not the live url, READY → PLAY, stopPropagation does not toggle expansion, no control on non-archive), and `src/tests/ui/catchup-recording.test.js` (the recorded arc). **Environment-red note (no live network):** the only reds are the **documented pre-existing no-network environment-red set** (CHANGELOG #17): `src/tests/ui/log-demo.test.js` (`expected "HLS not supported", received "levelLoadError"` — no live CDN) + downstream serial steps, `src/tests/ui/fmtchip-demo.test.js`, `src/tests/ui/live.test.js` (live portal unreachable), and the live-network integration tier including the new `src/tests/int/catchup.test.js` (arch/archDur shape over the live portal). These are **not** E21 regressions: the run adds no `src/server/**` or proxy code and no new external connectivity (the archive stream reuses the existing proxy + dual-engine play path). Integration: when outbound network was available mid-run (TASK-0075 validation) the full int tier ran **33/33 green** (9 files, incl. the new `catchup.test.js`); on the no-network passes it is 15 passed / 16 environment-red (transient sandbox condition, not a defect). Verified green locally at review: `npx vitest run src/tests/unit/{play,epgui,api}.test.js` → 150 passed.
- **Demo:** `test-results/e21-catchup-demo.webm` (~405 KB, committed on the run branch via TASK-0076), referenced from PR #43's `### Demo` as a clickable file-viewer (blob) link (private repo). Recorded arc: boot (fresh `http://localhost:3000` load via the Playwright `webServer`, idle, no cards/guides/Replay) → prepare (footer demo connect; scan cards, open each guide, stop on the first card whose expanded schedule carries a `.ch-replay`) → interact (focus the past archive row's `aria-label="Replay …"` Replay button, click it, assert the archived program plays through the normal player — `body.is-play`, `#now-info` = channel name, `#player-video` visible, `#player-idle` hidden) → revert runtime state (in-app `stopPlay`, clear cur/search/filter, legal phase walk, re-render grid so the open guide collapses, back to idle) → stop. E21 adds user-interactable behaviour, so it is **not** demo-exempt; the `### Demo` reference is present and accurate (confirmed at review).
- **Rules earned:** none (no terminal failures; first-attempt pass each, no persistent recovered near-miss).
- **Artifacts:** `CONVENTIONS.md` (§7 `Ch`/`CH_DEF` extended with `arch`/`archDur`, ADR-0035 schema home — ADR-side-linked), `src/client/api.js` (`mkXtCh` archive population + `mkM3uCh`/`mkDemoCh` defaults + demo archive synthesis, `ADR-0035`/`ADR-0036`), `src/client/play.js` (pure `getArchUrl` + `getStamp`/`getArchDur` helpers, exported on `window.IptvPlay`, `ADR-0036`), `src/client/ui.js` (`mkSchedRow` Replay control + `goReplay`/`onReplay` wired through `onGridClick`, `ADR-0036`), `src/client/app.css` (`.ch-replay` rules, `ADR-0036`), `docs/specs/catchup-archive.md` (new feature spec), `docs/adrs/ADR-003{5,6}-*.md`, `tasks/TASK-007{2,3,4,5,6}-*.md`, `src/tests/unit/{play,epgui,api,epgfetch}.test.js`, `src/tests/int/catchup.test.js`, `src/tests/ui/{catchup,catchup-demo,catchup-recording}.test.js`, `test-results/e21-catchup-demo.webm`, `README.md` (intro catch-up paragraph + new Catch-up (archive) Replay feature bullet). Traceability: all 4 of ADR-0035's `governs:` paths and all 11 of ADR-0036's resolve to tracked files at the branch tip, each carrying its `ADR-0035`/`ADR-0036` reference comment (`api.js` carries both; `CONVENTIONS.md` is ADR-side-linked only as a prose schema home); no E21 code references a deleted ADR; both ADRs stay `accepted` with non-empty `governs:`. The ADR-0036 demo test is named `catchup-recording.test.js` (the `catchup-demo.test.js` name was taken by TASK-0075's activation test); both are in `governs:` and carry the `ADR-0036` comment.
- **Notes:** branch `ai/e21-catchup-archive-replay`, PR **#43**, **stacked on the unmerged E20 branch** (`ai/e20-epg-program-reminders`, PR #41) → **E19** (`ai/e19-epg-now-next-guide`, PR #39) → **E18** (`ai/e18-product-memory-file`, PR #37) — cut from E20's tip, not fresh `main`, so PR #43's diff against `main` carries the E20+E19+E18 commits and depends on those PRs merging first (or together), mirroring prior stacked-run entries (E12–E15/E19/E20). TASK-0073's commit (`189ee8f`) accidentally swept in some `test-results/*` byte-regenerations (cosmetic diff noise from a UI regression run, not a correctness issue); later task commits were clean and left further regenerations unstaged (out of scope, mirroring prior runs). validate-agent wrote every per-task Test Results block into PR #43 and this REVIEW finalized it; the repo is private, so the demo is a clickable blob-viewer link. No open items remain.

---

## #20 — 2026-06-15 — Program reminders on the EPG: a Remind toggle on each upcoming schedule row + NOW/NEXT line, persisted store, timer-fired toast + best-effort browser Notification with jump-to-channel (ADR-0032/0033/0034)

- **Prompt:** Add program reminders on top of the EPG — a keyboard-focusable "Remind" toggle on each row of the expandable per-channel schedule (and the NOW/NEXT line) marks an upcoming program; reminders persist in a new localStorage-keyed store mirroring the favourites pattern and read the in-memory `IptvEpg` program data; a lightweight client timer checks pending reminders against program start times and, when one fires, surfaces an in-app toast plus a best-effort, permission-gated browser Notification so the user can jump to that channel; reminders can be cleared individually; degrade silently when no guide is loaded or notifications are denied; no new state-machine phase, no new playback engine, no new server route. (The E19 Phase-4 research winner.)
- **Outcome:** Shipped. All 5 tasks done and validated on the **first attempt each**. A purely **additive client store + render affordance + timer** built on the E19 EPG — no new state-machine phase, no new playback engine, no new server route. A new self-contained client module `src/client/rem.js` (`window.IptvRem`) holds the reminder list with **localStorage persistence** under a new key `iptv_rems` (mirroring the favourites pattern, ADR-0003), the pure builders/identity (`mkRem`, `key`), store ops (`list`/`has`/`add`/`rm`/`toggle`/`count`/`clear`/`load`), and the pure `due(now)` selector. A keyboard-focusable **Remind** `<button>` (`data-rem`, baseline-present `aria-pressed` per R-0001, state-flipping label) renders on each **upcoming** schedule row and on the NOW/NEXT line's NEXT part, toggled in place via `onGridClick` (propagation-stopped so it never plays the channel or toggles expansion — mirroring the fav star). A lightweight coarse timer (`remTick` = 20s) wired on page load in `src/client/main.js` reads `IptvRem.due(Date.now())` each tick, fires each newly-due reminder **exactly once** through a new minimal in-app toast surface (`aria-live`, auto-dismiss after `toastMs` = 8s, **Watch** action reusing the existing select+play path, dismiss control) plus a best-effort, permission-gated browser `Notification` (permission requested at most once on the user's first-reminder gesture, never on load), then removes it from the store. Everything degrades silently — no guide, no localStorage, no `Notification`, or denied permission. ADR↔code traceability holds for ADR-0032/0033/0034; the mid-run gap left by TASK-0069 (ADR-0034 governs `src/client/main.js`) was **closed** by TASK-0070 (main.js carries `// ADR: ADR-0034`).
- **ADRs:** ADR-0032 (`docs/adrs/ADR-0032-reminders-store.md`, **accepted**) — reminders are a client-side, localStorage-persisted store mirroring the favourites pattern, keyed off the in-memory EPG program data; adds `src/client/rem.js` (`window.IptvRem`), the new key `iptv_rems` (`remsKey` in `cfg.js`), the `Rem` `{chId,start,title}` shape derived from the canonical `Prg`, and the pure `due(now)` selector; no account coupling, no server route, no new phase. `governs:` declares `src/client/rem.js`, `src/client/cfg.js`, `src/index.html`, `src/tests/unit/rem.test.js`. ADR-0033 (`docs/adrs/ADR-0033-remind-toggle-on-epg-rows.md`, **accepted**) — render the Remind toggle as a keyboard-focusable `aria-pressed` button on each upcoming schedule row and the NOW/NEXT line, reusing `mkSchedRow`/`mkNowNext` and the proven fav-toggle in-place-update + propagation-stop model; `aria-pressed` is baseline-present (R-0001). `governs:` declares `src/client/ui.js`, `src/client/app.css`, `src/tests/unit/remui.test.js`, `src/tests/ui/reminders.test.js`. ADR-0034 (`docs/adrs/ADR-0034-reminder-firing-toast-notification.md`, **accepted**) — a lightweight client timer fires due reminders as an in-app toast + best-effort permission-gated browser Notification with a jump-to-channel action, degrading silently; the toast reuses the existing select+play path (`setCur` + `saveSt('sel')` + `go('PLAY')`), no new engine/phase. `governs:` declares `src/client/ui.js`, `src/client/app.css`, `src/client/cfg.js`, `src/client/main.js`, `src/index.html`, `src/tests/unit/remfire.test.js`, `src/tests/ui/reminders.test.js`, `src/tests/ui/reminders-demo.test.js`. All three are first-in-force this evolution; no prior ADR is contradicted, superseded, or deleted.
- **Tasks:** 5 done, 0 failed, 0 blocked. TASK-0067 — reminders store `rem.js` (`Rem` shape, builders, store ops, `load()` + `iptv_rems` persistence via `remsKey`, pure `due(now)`, `index.html` load order after `epg.js` before `ui.js`) — commit `03805b4`, first-attempt · TASK-0068 — Remind toggle on each upcoming schedule row + NOW/NEXT line (`mkSchedRow`/`mkNowNext`, baseline `aria-pressed`, styling, in-place `onGridClick` toggle, propagation-safe) — commit `71a3d48`, first-attempt · TASK-0069 — reminder firing surface (minimal `aria-live` toast in `ui.js`+`app.css`, jump-to-channel via the existing select+play path, best-effort permission-gated Notification with the permission request gated to the first-reminder gesture) — commit `e1a3cac`, first-attempt · TASK-0070 — client reminder timer wired on page load (`main.js` + `remTick` interval in `cfg.js`: tick reads `due(now)`, fires each newly-due reminder once, removes it; boot restores via `IptvRem.load()`; guarded no-op when globals absent) — commit `553e651`, first-attempt · TASK-0071 — demo recording of the reminder set → fire → notify → clear arc — commit `acf0867`, first-attempt.
- **Tests:** 796 unit (Vitest, 37 files) — all green, including the E20-specific `src/tests/unit/rem.test.js` (26: `mkRem`/`key` builders, `add`/`has`/`rm`/`toggle` identity + de-dup, `list()` fresh-copy, `clear()`, persistence under `iptv_rems` + swallow-on-throw, `load()` validate/drop-malformed/never-throw/normalize, `due(now)` grace-window select + purity + no-write + `Date.now()` default), `src/tests/unit/remui.test.js` (13: `mkSchedRow`/`mkNowNext` Remind-button emission with baseline `aria-pressed`, upcoming-only, guarded-absent, NEXT-only on now/next, state-flipping HTML-escaped label, `data-rem` format, `onGridClick`→`toggle`+stopPropagation, `toggleRem` in-place flip both directions, R-0001 baseline-present), `src/tests/unit/remfire.test.js` (17: `fireRem`/`mkToast`/`onToastClick`/`rmToast`/`goRemWatch`/`fireNote`/`mkNote`/`askRemPerm` — toast build + auto-dismiss + escape, Watch select+play + no-op-when-PLAY + unknown-channel no-op, dismiss, Notification only-when-granted + skip-on-denied/absent/default + toast-still-fires, permission once on first-reminder SET gesture only, guarded degradation), `src/tests/unit/remtimer.test.js` (9: boot `load()`, exactly one interval at `S.remTick`, fire-each-once + remove + future-kept, no-re-fire across ticks, real-faked-clock auto-tick, empty/future-only no-op, guarded no-op when `IptvRem`/`fireRem` absent). UI (Playwright): 232 product UI tests pass, including `src/tests/ui/reminders.test.js` (NOW/NEXT baseline toggle, keyboard-activate flips `aria-pressed` without playing, schedule-row toggle flips in place without playing/collapsing, current-airing row carries no toggle, firing shows a toast + granted Notification, Watch plays + dismisses, dismiss removes without playing) and `src/tests/ui/reminders-demo.test.js` (the recorded arc). **Environment-red note (no live network):** the only reds are the **documented pre-existing no-network environment-red set** (CHANGELOG #17): `src/tests/ui/log-demo.test.js` (`expected "HLS not supported", received "levelLoadError"` — no live CDN) + 3 downstream serial steps, `src/tests/ui/live.test.js` (live portal unreachable), and the live-network integration tier (15 passed / 13 environment-red — `e2e`/`epg`/`redir`/`remux`/`xtream` live-portal connect). These are **not** E20 regressions: this run touches no `src/server/**`, player/CDN, or proxy code and adds no external connectivity (client-only store + render + timer). Verified green locally at review: `npx vitest run src/tests/unit/{rem,remui,remfire,remtimer}.test.js` → 65 passed.
- **Demo:** `test-results/e20-reminders-demo.webm` (~576 KB, committed on the run branch via TASK-0071), referenced from PR #41's `### Demo` as a clickable file-viewer (blob) link (private repo). Recorded arc: boot (fresh load, idle, no cards, no toggles, no toasts) → prepare (footer demo connect; synthetic guide renders so each card carries a NOW/NEXT line with a Remind toggle; a mocked-granted `Notification` is installed before page scripts, no real OS prompt) → interact (keyboard-activate the NEXT Remind toggle — `aria-pressed` flips `true`, no play/expand; fire the reminder through the timer path `window.IptvUi.fireRem` so the in-app toast appears in the `aria-live` region with title + Watch + dismiss and the granted Notification fires; the toast's Watch jumps to + plays the reminded channel and the toast dismisses; clear the reminder individually — `aria-pressed` flips back to `false`) → revert runtime state (clear stored reminders, stop playback, clear current channel / search / filter, return to idle — in-app teardown) → stop. E20 adds user-interactable behaviour, so it is **not** demo-exempt; the `### Demo` reference is present and accurate (confirmed at review).
- **Rules earned:** none (no terminal failures; first-attempt pass each, no persistent recovered near-miss).
- **Artifacts:** `src/client/rem.js` (new, `// ADR: ADR-0032`), `src/client/cfg.js` (`remsKey`/`toastMs`/`remTick` in `S`, `ADR-0032`/`ADR-0034`), `src/client/ui.js` (`mkSchedRow`/`mkNowNext` Remind toggle + `toggleRem` + the toast surface `fireRem`/`mkToast`/`onToastClick`/`rmToast`/`goRemWatch`/`fireNote`/`mkNote`/`askRemPerm`, `ADR-0033`/`ADR-0034`), `src/client/main.js` (reminder timer wired on load + boot `IptvRem.load()`, `ADR-0034`), `src/client/app.css` (Remind-toggle + `.rem-toast*` rules, `ADR-0033`/`ADR-0034`), `src/index.html` (`rem.js` script after `epg.js` before `ui.js` + the `#rem-toasts` `aria-live` region, `<!-- ADR: ADR-0032 -->`/`ADR-0034`), `docs/specs/reminders.md` (new feature spec), `docs/adrs/ADR-003{2,3,4}-*.md`, `tasks/TASK-006{7,8,9}-*.md` + `tasks/TASK-007{0,1}-*.md`, `src/tests/unit/{rem,remui,remfire,remtimer}.test.js`, `src/tests/ui/{reminders,reminders-demo}.test.js`, `test-results/e20-reminders-demo.webm`, `README.md` (intro program-reminders paragraph + new Program reminders feature bullet). Traceability: all four of ADR-0032's `governs:` paths, all four of ADR-0033's, and (after the TASK-0070 gap-close) ADR-0034's listed paths resolve to tracked files at the branch tip, each carrying its `ADR-NNNN` reference comment (`ui.js`/`cfg.js`/`app.css`/`index.html` carry several); no E20 code references a deleted ADR; all three ADRs stay `accepted` with non-empty `governs:`. **Open item (status-fix + traceability, routed to the orchestrator):** see Notes.
- **Notes:** branch `ai/e20-epg-program-reminders`, PR **#41**, **stacked on the unmerged E19 branch** (`ai/e19-epg-now-next-guide`, PR #39) → **E18** (`ai/e18-product-memory-file`, PR #37) — PR #41's diff against `main` carries the E19+E18 commits and depends on them merging first (or together), mirroring prior stacked-run entries (E12/E13/E14/E15/E19). The non-deterministic `test-results/*.webm|*.png` byte-regenerations from the UI regression runs were deliberately left **unstaged** (out of scope, mirroring prior runs). validate-agent wrote every per-task Test Results block into PR #41 and this REVIEW finalized it; the repo is private, so the demo is a clickable blob-viewer link. **Two open items routed to the orchestrator at finalization (review-agent does not edit task files or ADRs):** (1) **status-fix** — TASK-0068's task file carries `attempts: 0` for a clean first-attempt pass (every other E20 task records `attempts: 1`); reported as a `status-fix` discrepancy (identical to the E19 TASK-0064 correction). (2) **traceability-gap** — `src/tests/unit/remtimer.test.js` was added by TASK-0070 and carries a `// ADR: ADR-0034` reference comment, but is **not** listed in ADR-0034's `governs:` (ADR-0034 lists `remfire.test.js` only); the `governs:` list should be trued up to add `src/tests/unit/remtimer.test.js`. Neither affects code or test correctness — both the deterministic unit (796/796) and UI (232) gates are green.

---

## #19 — 2026-06-15 — Add an EPG: now/next per channel card + expandable per-channel schedule (Xtream short-EPG + XMLTV via `tvg-id`) (ADR-0030/0031)

- **Prompt:** Add an Electronic Program Guide showing what is on now and next per channel plus an expandable per-channel schedule, sourcing data from the Xtream short-EPG endpoint (`get_simple_data_table`) on the Xtream path and an XMLTV guide via each M3U entry's `tvg-id` on the M3U path, reusing the canonical schema, flat state machine, sidebar/channel-grid layout, and localStorage conventions, adding no new playback engine. (The E18 Phase-4 research winner.)
- **Outcome:** Shipped. All 5 tasks done and validated on the **first attempt each**. A purely **additive data-fetch and render surface** — no new playback engine, no new state-machine phase, no new localStorage key. A new self-contained client module `src/client/epg.js` (`window.IptvEpg`) holds an in-memory, session-scoped program-guide store (keyed by channel id) plus the two pure parsers (`parsXtEpg`, `parsXmltv`) and the now/next/schedule selectors (`getNowNext`, `getSched`), mirroring the failure-log (ADR-0027) posture. After a successful connect, guide data is fetched **best-effort and non-blocking** through the existing `/api/xtream?url=<encoded>` proxy — Xtream short-EPG per channel (batched), the M3U XMLTV guide via `tvg-id`, and a synthetic guide for demo mode — and never fails or delays browsing. Each channel card gains a NOW/NEXT line (absent when no guide is loaded, ADR-0025 contextual presence) and a keyboard-focusable expand control that reveals the channel's upcoming schedule without starting playback (presentational `is-expanded`, ADR-0028 panel-open model). ADR↔code traceability holds fully and the run carries **no open items** beyond a task-file `attempts` correction routed to the orchestrator (see Notes).
- **ADRs:** ADR-0030 (`docs/adrs/ADR-0030-epg-data-and-fetch.md`, **accepted**) — EPG is a client-side, in-memory, session-scoped program-guide store fed by Xtream short-EPG and XMLTV through the existing CORS proxy; reuses the already-declared canonical `Prg`/`PRG_DEF` schema (CONVENTIONS §7, its first consumer), adds no new server route, no new playback engine, no new localStorage key, and no new state-machine phase. **Documents the deliberate divergence** from CONVENTIONS.md §3's stale server-side `src/server/epg.js` + `/api/epg` sketch: EPG fetch is client-side via the existing generic proxy (mirroring `api.js`), as the prompt scopes it — the §3 sketch is not followed by design (ADR-0030 "Ruled out: a server-side EPG cache/route"). `governs:` declares `src/client/{epg,api,ui,main}.js`, `src/index.html`, and the three EPG data-layer test files. ADR-0031 (`docs/adrs/ADR-0031-epg-card-surface.md`, **accepted**) — render the EPG entirely within the existing grid/card UI: a now/next line on `mkCard` and a presentational expandable per-channel schedule, reading `window.IptvEpg` at render time. `governs:` declares `src/client/ui.js`, `src/client/app.css`, and the three EPG-UI test files. Both ADRs are first-in-force this evolution; no prior ADR is contradicted, superseded, or deleted.
- **Tasks:** 5 done, 0 failed, 0 blocked. TASK-0062 — `epg.js` store + parsers (`parsXtEpg`, `parsXmltv`) + selectors (`getNowNext`, `getSched`) + `index.html` load order — commit `791fe56`, first-attempt · TASK-0063 — best-effort EPG fetch wired into the connect flow (Xtream short-EPG, M3U XMLTV via `tvg-id`, demo synthetic) through the existing proxy — commit `6650d42`, first-attempt · TASK-0064 — now/next line on the channel card (`mkCard`, render-on-guide, styling) — commit `6a83768`, first-attempt · TASK-0065 — expandable per-channel schedule view (expand control + schedule list, presentational `is-expanded`, playback-safe) — commit `c0869dd`, first-attempt · TASK-0066 — demo recording of the EPG now/next + expandable schedule — commit `c9bd292`, first-attempt.
- **Tests:** 731 unit (Vitest, 33 files) — all green, including the E19-specific `src/tests/unit/epg.test.js` (31: parser timestamp coercion / base64 decode / malformed-drop / sort, store set/setAll/get-copy/has/count/clear, `getNowNext` now/next/gap/boundary, `getSched` current-plus-upcoming), `src/tests/unit/epgfetch.test.js` (16: Xtream proxied-URL build + batching/budgeting, M3U XMLTV fetch + `setAll`, demo synthetic, swallow-on-reject/non-ok, never-throws, `getTvgUrl`, onDone re-render hook), and `src/tests/unit/epgui.test.js` (22: now/next render + markers + HTML-escape + graceful degradation + contextual absence; expand control `aria-expanded`/label/`data-exp`, schedule rows local-time range + category + current marker, escape, and the card's only play target stays the card body). UI (Playwright): 217 product UI tests pass, including `src/tests/ui/epg.test.js` (now/next line on demo cards, decorative/clickable separation, expand toggles schedule without playing, card body still plays after expanding) and `src/tests/ui/epg-demo.test.js` (the recorded arc). **Environment-red note (no live network):** the only reds are the **documented pre-existing no-network environment-red set** (CHANGELOG #17): `src/tests/ui/log-demo.test.js` (`expected "HLS not supported", received "levelLoadError"` — no live CDN) + 3 downstream serial steps, `src/tests/ui/live.test.js` (live portal unreachable), and the live-network integration tier including the new `src/tests/int/epg.test.js`. These are **not** E19 regressions. Integration: when live outbound network was available mid-run (TASK-0064 validation), the full int tier ran **30/30 green** (8 files, incl. the new `epg.test.js` exercising EPG over the live Xtream portal + the public XMLTV mirror), within the documented ≥1-of-5 sampled-stream tolerance; on the no-network passes it is 15 passed / 13 environment-red. Verified green locally at review: `npx vitest run src/tests/unit/{epg,epgfetch,epgui}.test.js` → 69 passed.
- **Demo:** `test-results/e19-epg-demo.webm` (~477 KB, committed on the run branch via TASK-0066), referenced from PR #39's `### Demo` as a clickable file-viewer (blob) link (private repo). Recorded arc: boot (fresh load, idle, no guide) → prepare (footer demo connect: 31 cards + synthetic guide, 31 now/next lines) → interact (the first card's NOW/NEXT line with real program titles; the expand control reveals the schedule without starting playback — no PLAY phase, video hidden; activating it again collapses) → revert runtime state (in-app teardown back to idle) → stop. E19 adds user-interactable behaviour, so it is **not** demo-exempt; the `### Demo` reference is present and accurate (confirmed at review).
- **Rules earned:** none (no terminal failures; first-attempt pass each, no persistent recovered near-miss).
- **Artifacts:** `src/client/epg.js` (new, `// ADR: ADR-0030`), `src/client/api.js` (`loadEpg`/`getTvgUrl` + Xtream/M3U/demo EPG wiring, `ADR-0030`), `src/client/ui.js` (`mkCard` now/next line + `mkSched`/`mkSchedRow` expand control + schedule, `ADR-0030`/`ADR-0031`), `src/client/main.js` (EPG fetch hooked into the post-connect flow + re-render, `ADR-0030`), `src/client/app.css` (now/next + `.ch-exp`/`.ch-sched` + `is-expanded` rules, `ADR-0031`), `src/index.html` (`epg.js` script before `ui.js`, `<!-- ADR: ADR-0030 -->`), `docs/specs/epg.md` (new feature spec) + `docs/specs/project.md` feature-spec list entry + `src/client/**` note (added Phase 1), `docs/specs/integration-testing.md` (new **EPG tier** reference section mirroring the XMLTV guide endpoint `https://i.mjh.nz/PlutoTV/us.xml` declared as a constant in `src/tests/int/epg.test.js`, added this REVIEW), `docs/adrs/ADR-003{0,1}-*.md`, `tasks/TASK-006{2,3,4,5,6}-*.md`, `src/tests/unit/{epg,epgfetch,epgui}.test.js`, `src/tests/int/epg.test.js`, `src/tests/ui/{epg,epg-demo}.test.js`, `test-results/e19-epg-demo.webm`, `README.md` (intro EPG paragraph + new Program guide (EPG) feature bullet). Traceability: all 8 of ADR-0030's `governs:` paths and all 5 of ADR-0031's resolve to tracked files at the branch tip, each carrying its `ADR-0030`/`ADR-0031` reference comment (`ui.js` carries both); no E19 code references a deleted ADR; both ADRs stay `accepted` with non-empty `governs:`.
- **Notes:** branch `ai/e19-epg-now-next-guide`, PR **#39**, **stacked on the unmerged E18 branch** (`ai/e18-product-memory-file`, PR #37) — cut from `ai/e18-product-memory-file`, not fresh `main`, so PR #39's diff against `main` carries E18's commits and depends on PR #37 merging first (or together), mirroring prior stacked-run entries (E12/E13/E14/E15). The integration-testing-spec EPG endpoint question raised in the run manifest is **resolved**: the spec now mirrors the XMLTV reference endpoint as an EPG tier (review-agent may edit `docs/specs/`). The CONVENTIONS.md §3 stale server-side `src/server/epg.js` + `/api/epg` sketch was deliberately **not** followed (client-side fetch via the existing proxy), documented coherently in ADR-0030's Decision + "Ruled out" — the code is correct, the sketch is stale. **Status-fix open at finalization:** TASK-0064's task file carried `attempts: 0` for a clean first-attempt pass (every other E19 task records `attempts: 1`); review reported it as a `status-fix` discrepancy for the orchestrator to correct (review-agent does not edit task files). The non-deterministic `test-results/*.webm|*.png` byte-regenerations from the UI regression runs were deliberately left **unstaged** (out of scope, mirroring prior runs). validate-agent wrote every per-task Test Results block into PR #39 and this REVIEW finalized it; the repo is private, so the demo is a clickable blob-viewer link.

---

## #18 — 2026-06-15 — Keep a durable product-project memory file at `docs/MEMORY.md`, referenced from product docs (ADR-0029)

- **Prompt:** Keep a `Memory.md` of the product project in the `docs/` folder and use it as memory that Claude references. (Consumed the parked BACKLOG idea "Product-project memory file under docs/ that Claude references" — 2026-06-13; that entry was removed from `BACKLOG.md` on finalization.)
- **Outcome:** Shipped. The single task completed and validated on the **first attempt**. A **documentation-only** change — no product behaviour, no interactive surface. `docs/MEMORY.md` now exists as the product project's durable, authoritative memory: a Markdown document with a self-describing header (durable product-project memory for any human or AI contributor to consult), an explicit boundary table distinguishing what belongs in memory vs. `docs/specs/`, `docs/adrs/`, `README.md`, `CHANGELOG.md`, `docs/notes/`, and the harness files, and a seeded "Durable facts" section carrying the product/UI name "IPTV Broadcast Console" and the code-non-derivable codename / Fly.io app name `teeatr`. The "Claude references it" requirement is wired entirely on the product side (no harness file touched): `docs/specs/project.md` gained a Product memory pointer (Phase 1) and `README.md` names it as the product's durable project memory (this REVIEW). A harness-side pointer is explicitly out of scope (future coreflow-agent change). ADR↔code traceability holds and the run carries **no open items**.
- **ADRs:** ADR-0029 (`docs/adrs/ADR-0029-product-memory-file.md`, **accepted**) — adopt a single durable product-project memory file at `docs/MEMORY.md` (uppercase, `docs/` root) and wire its reference entirely on the product side: `docs/specs/project.md` (spec-agent-owned) points at it; `README.md` (review-agent-owned) names it as durable project memory. `CLAUDE.md` / `CORE_FLOW.md` are harness-owned and are NOT edited by this product run — a harness-side pointer is captured for a future coreflow-agent change. `docs/MEMORY.md` is product-owned, maintained through the pipeline, not owned by backlog- or coreflow-agent. `governs:` declares four paths — `docs/MEMORY.md`, `docs/specs/product-memory.md`, `docs/specs/project.md` (all Markdown, ADR-side-linked only per CORE_FLOW.md §3), and `src/tests/unit/memory.test.js` (carries the `// ADR: ADR-0029` code-side comment). First-in-force this evolution; no prior ADR contradicted, superseded, or deleted.
- **Tasks:** 1 done, 0 failed, 0 blocked. TASK-0061 — Create `docs/MEMORY.md` durable product-project memory + product-side reference (new `docs/MEMORY.md`, new `src/tests/unit/memory.test.js`; the `docs/specs/project.md` pointer was added in Phase 1) — commit `c265c23`, **first-attempt pass**.
- **Tests:** 662 unit (Vitest, 30 files) — all green, including the new `src/tests/unit/memory.test.js` (5: `docs/MEMORY.md` exists/non-empty, begins with a top-level `# ` heading, references the boundary artifacts specs/ADRs/README/CHANGELOG, contains the seeded `teeatr` fact, and `docs/specs/project.md` points at `docs/MEMORY.md`) — the test reads the files from disk so the durable facts are enforced to live in `docs/MEMORY.md` itself. UI (Playwright): 207 product UI tests passed; the single red — `src/tests/ui/log-demo.test.js` (`expected "HLS not supported", received "levelLoadError"`) plus 3 downstream serial steps — is the **documented pre-existing environment-red set** (CHANGELOG #17 environment-red note: this sandbox has no live outbound CDN, so in-browser hls.js emits a network error instead of the asserted failure path). It is **not** an E18 regression: this docs-only task touches zero `src/client/**`, `src/server/**`, `src/index.html`, or player/CDN code. Integration: 25/25 green (7 files, live network) — run as the regression gate; this docs-only run adds no external connectivity (live-network sampling within the documented tolerances). Verified green locally at review: `npx vitest run src/tests/unit/memory.test.js` → 5 passed.
- **Demo:** exempt — `No demo — documentation-only change, no interactive surface`. PR #37's `### Demo` section carries that exact note (CORE_FLOW.md §3 scope cutoff: a documentation-only change with no user-interactable behaviour is demo-exempt).
- **Rules earned:** none (no terminal failures; first-attempt pass, no recovered near-miss).
- **Artifacts:** `docs/MEMORY.md` (new), `src/tests/unit/memory.test.js` (new, `// ADR: ADR-0029`), `docs/specs/product-memory.md` (new feature spec) + `docs/specs/project.md` (new Product memory section + feature-spec list entry, added Phase 1), `docs/adrs/ADR-0029-product-memory-file.md`, `tasks/TASK-0061-product-memory-file.md`, `README.md` (Architecture pointers section now names `docs/MEMORY.md` as the product's durable project memory), `BACKLOG.md` (consumed "Product-project memory file under docs/ that Claude references" entry removed). Traceability: all four of ADR-0029's `governs:` paths resolve to tracked files at the branch tip — the three Markdown files are linked from the ADR side only (Markdown carries no native ADR comment), and `src/tests/unit/memory.test.js` carries its `// ADR: ADR-0029` reference; no E18 code references a deleted ADR; ADR-0029 stays `accepted` with non-empty `governs:`.
- **Notes:** branch `ai/e18-product-memory-file`, PR **#37**, cut fresh from `main` (E17 merged at `c04970b`; the add-research-phase harness PR #36 already merged beneath it). The non-deterministic `test-results/*.webm|*.png` byte-regenerations produced by the UI regression run were deliberately left **unstaged** (out of scope, mirroring E14/E16/E17). validate-agent wrote the per-task Test Results block into PR #37 and the orchestrator's REVIEW finalized it; the repo is private. No open items remain.

---

## #17 — 2026-06-13 — Log channel-playback failures, surfaced via a log button beside the account button (ADR-0027/0028)

- **Prompt:** Create a new feature that enables logging only failures when selecting a channel throws an error for playing it — logs must be a button besides the account button.
- **Outcome:** Shipped. All 5 tasks done and validated; 4 first-attempt, 1 (TASK-0058) on attempt 2 after a recovered layout-adjacency near-miss. A new user-facing diagnostic: every fatal channel-playback failure (and only a failure) is recorded into an in-memory, session-scoped log, and a new **Log** button beside the account button opens a slide-in panel that lists the failed channels with their detail and time, with a Clear action and a count badge. Successful plays are structurally never logged (single `onEngErr` choke point). No new state-machine phase, no persistence, no account coupling. ADR↔code traceability holds fully and the run carries **no open items**.
- **ADRs:** ADR-0027 (`docs/adrs/ADR-0027-playback-failure-log.md`, **accepted**) — capture only channel-playback failures into an in-memory, session-scoped failure log. A self-contained `src/client/errlog.js` exposes `window.IptvErrLog` with `add`/`list`(newest-first copy)/`count`/`clear`/`mkEntry`(pure builder), capping at 50 (drops oldest). The **single** writer is `onEngErr(msg)` in `src/client/play.js` — the already-shared fatal choke point for fatal hls.js errors, mpegts.js errors, and the "not supported" dead-ends — so successes cannot reach it; the call is guarded (`if (window.IptvErrLog) …`) for test isolation, `errlog.js` loads before `play.js`/`ui.js`. Not persisted, not coupled to the account store (rules out localStorage; rules out a second capture site / new phase). ADR-0028 (`docs/adrs/ADR-0028-playback-log-button-panel.md`, **accepted**) — surface the log via a top-bar `#log-btn` mounted in `.content-head` immediately before `#acct-btn` (source order: log button first, account button last, account keeps `margin-left:auto`), with a hidden-when-zero `#log-count` badge folded into the button's accessible name. A `#log-panel` slide-in aside + `#log-scrim` parallel to the account panel (ADR-0014), opened/closed by a single `is-open` class via `setLog(open)`, closable by close button / scrim / Escape (shared keydown handler). `rndLog()` renders the badge + newest-first entry rows (HTML-escaped) or a calm empty state; `onLogClear` empties + re-renders; `onEngErr` re-renders the log after capture. Both ADRs are first-in-force this evolution; no prior ADR is contradicted, superseded, or deleted. The visual right-edge cluster renders `[log-btn][theme-toggle][acct-btn]` via CSS flex `order` (by design — preserves ADR-0028's source order while keeping ADR-0019's toggle↔account adjacency).
- **Tasks:** 5 done, 0 failed, 0 blocked. TASK-0056 — in-memory failure-log store `src/client/errlog.js` (`add`/`list`/`count`/`clear`/`mkEntry`, 50-cap, num=0 preserved) — commit `2c0ded6`, first-attempt · TASK-0057 — capture at the `onEngErr` choke point (`src/client/play.js`, `src/index.html` load order errlog→play→ui) — commit `ba366a3`, first-attempt · TASK-0058 — log button beside the account button + slide-in panel shell, CSS, open/close (`src/index.html`, `src/client/app.css`, `src/client/ui.js`) — commit `32e31a5`, **attempt 2** (see near-miss below) · TASK-0059 — render panel + badge from `IptvErrLog`, Clear action, capture re-render (tests added; production code already present) — commit `f229a57`, first-attempt · TASK-0060 — demo recording (`src/tests/ui/log-demo.test.js` → `test-results/e17-failure-log-demo.webm`) — commit `93a2133`, first-attempt.
- **Tests:** 657 unit (Vitest) — all green, including the E17-specific `src/tests/unit/errlog.test.js` (16: `mkEntry` normalization incl. partial/null/num=0, add/count accumulate, 50-cap drops oldest, `list()` newest-first copy, `clear()`), `src/tests/unit/playcap.test.js` (15: one entry per fatal path via hls.js/mpegts/"not supported", null `ST.cur` → "Unknown channel", absent-`IptvErrLog` no-throw, success path records nothing), and `src/tests/unit/logui.test.js` (34: `mkEL` resolves new `EL` entries, `setLog`/`onLogBtn`/`onLogClose` + Escape toggle, `rndLog` empty/non-empty/newest-first/HTML-escape/accessible-name, `onLogClear`, capture→rndLog wiring end-to-end). UI (Playwright): `src/tests/ui/log.test.js` (13: button beside account, slide-in open/close/scrim/Escape/keyboard, badge + entry rows, Clear → empty state) + `src/tests/ui/log-demo.test.js` (7: the recorded arc). **Environment-red note:** this sandbox has no live outbound network/CDN, so a documented pre-existing environmental red set persists across every run (UI `chips.test.js:163` mpegts-CDN, `live.test.js:66`/`:85` live-portal, `grid-align-demo.test.js:239` revert + serial `:268` skip; the live-network integration tier ~13 reds). These are **not** E17 regressions and are out of scope to fix; the full unit suite is 657/657 green and all E17-specific unit + UI tests pass. Verified green locally at review: `npx vitest run src/tests/unit/{errlog,playcap,logui}.test.js` → 65 passed.
- **Demo:** `test-results/e17-failure-log-demo.webm` (~721 KB, committed on the run branch), referenced from PR #34's `### Demo` as a clickable file-viewer link (private repo). Recorded arc: boot (fresh load via the Playwright `webServer`; `#log-btn` asserted beside `#acct-btn`, badge hidden, panel off-screen) → prepare (connect demo mode, 31 cards) → interact (click a demo channel; the genuine `onGridClick → loadPlay → runHls → onEngErr('HLS not supported')` path records one real `IptvErrLog` entry; badge shows 1; open the panel from the button beside the account button; the entry row shows the channel name + `001` + detail; click Clear → empty state + hidden badge) → revert runtime state (close panel, `stopPlay`, clear err/cur, ERR→INIT, re-render idle) → stop.
- **Rules earned:** none (no terminal failures).
- **Recovered near-miss (recorded):** TASK-0058, tag `layout-adjacency-regression` — inserting `#log-btn` between `#theme-toggle` and `#acct-btn` in source order (required by ADR-0028) pushed the theme toggle ~88px from the account button, breaking the untouched `themetoggle.test.js:31` invariant (ADR-0019, toggle must sit <20px left of the account button). Fixed purely in `app.css` with flex `order` on the right-edge cluster: source order stays log→toggle→account (ADR-0028, `log.test.js` still sees `#log-btn` left of `#acct-btn`) while the cluster renders `[log-btn][theme-toggle][acct-btn]`, the toggle one `--s2` gap left of the account button (ADR-0019 satisfied). No test was edited. First occurrence of this tag — recorded in `failures/NEAR-MISSES.md`, below the recurrence threshold, so no rule earned.
- **Artifacts:** `src/client/errlog.js` (new), `src/client/play.js` (`onEngErr` capture + `rndLog` re-render), `src/client/ui.js` (`EL` lbtn/lcnt/lpnl/lscr/lcls/lclr/llst, `setLog`/`onLogBtn`/`onLogClose`/`onLogClear`/`rndLog`/`mkLogRow`/`fmtTime`, exported on `window.IptvUi`), `src/client/app.css` (`.log-btn`/`.log-count`/`.log-scrim`/`.log-panel`/`.log-*` rules + the flex-`order` right-edge cluster), `src/index.html` (`#log-btn` before `#acct-btn`, `#log-scrim`+`#log-panel`, `errlog.js` script before `play.js`/`ui.js`), `docs/specs/playback-failure-log.md` (new) + `docs/specs/project.md` feature-spec list entry, `docs/adrs/ADR-002{7,8}-*.md`, `tasks/TASK-005{6,7,8,9}-*.md` + `tasks/TASK-0060-*.md`, `src/tests/unit/{errlog,playcap,logui}.test.js`, `src/tests/ui/{log,log-demo}.test.js`, `test-results/e17-failure-log-demo.webm`, `README.md` (new failure-log feature bullet + dual-engine-player note), `failures/NEAR-MISSES.md` (TASK-0058 row). Traceability: all 5 of ADR-0027's `governs:` paths and all 7 of ADR-0028's resolve to tracked files at the branch tip, each carrying its `ADR-0027`/`ADR-0028` reference comment (`play.js` and `index.html` carry both); no E17 code references a deleted ADR; both ADRs stay `accepted` with non-empty `governs:`.
- **Notes:** branch `ai/e17-channel-error-log-button`, PR **#34**, cut fresh from `main` (merge-base at `ffc9422`, the merged near-miss-recording harness PR). The non-deterministic `test-results/*.webm|*.png` byte-regenerations from prior runs produced by the UI run were deliberately left **unstaged** (out of scope, mirroring E16). validate-agent wrote every per-task Test Results block and the orchestrator finalized the PR via MCP; the repo is private, so the demo is referenced as a clickable blob-viewer link. No open items remain.

---

## #16 — 2026-06-13 — Reorganize the repo: product source under `src/`, product docs under `docs/` (ADR-0026)

- **Prompt:** Move the generated product documentation (specs, implementation notes, adrs) into a `docs/` folder and the actual coded project into a `src/` folder — `docs/` holds product documentation only (NOT the harness engine, which stays at root), and `README.md`/`CHANGELOG.md` stay at the repo root. (Consumed the refined parked BACKLOG idea "Reorganize repo into docs/ and src/ top-level folders"; that entry was removed from `BACKLOG.md` on finalization.)
- **Outcome:** Shipped. A **pure structural relocation** — no product behavior change. Both tasks completed and validated on the **first attempt each**. All runtime/application source and its tests now live under a top-level `src/` (`src/index.html`, `src/client/**`, `src/server/**`, `src/tests/{unit,ui,int}/**`); the generated product documentation lives under a top-level `docs/` (`docs/specs/**` incl. `project.md`, `docs/adrs/**`, and a new `docs/notes/` home for implementation notes). Every path reference — tooling config/scripts, ADR `governs:` + prose, and `docs/specs/project.md` canonical commands — was rewired so the app and the full test suite keep working unchanged. The harness engine (`CORE_FLOW.md`, `CLAUDE.md`, `.claude/**`, `tasks/`, `failures/`, `BACKLOG.md`) and `README.md`/`CHANGELOG.md`/`CONVENTIONS.md` stay at root, and the test-runner configs (`vitest.config.js`, `vitest.int.config.js`, `playwright.config.js`) + `package.json` stay at root by tooling convention with their internal globs pointing at `src/…`.
- **ADRs:** ADR-0026 (`docs/adrs/ADR-0026-relocate-src-docs-layout.md`, **accepted**) — relocate product source under `src/` and product documentation under `docs/`. Preserves the relative offsets of `server/`/`client/`/`index.html` under `src/` so `server/srv.js`'s `ROOT = path.join(__dirname, '..')` static-dir resolution (ADR-0002) keeps working with **no code change**. Its `governs:` declares the directory roots (`src/index.html`, `src/client/`, `src/server/`, `src/tests/`, the four root config/package files, `docs/specs/`, `docs/adrs/`, `docs/notes/`). No accepted ADR is contradicted, superseded, or deleted — every prior ADR's `governs:` was rewritten from `client/…`/`tests/…`/`specs/…`/`adrs/…` to the `src/…`/`docs/…` layout, and every governed `src/` file retained its `// ADR: ADR-NNNN` reference comment through the `git mv`.
- **Tasks:** 2 done, 0 failed, 0 blocked. TASK-0054 — Relocate product source tree under `src/` and rewire tooling (`src/{index.html,client,server,tests}`, `package.json` `main`/`start`/`test:int`, `vitest.config.js`/`vitest.int.config.js` include globs, `playwright.config.js` `testDir`/`webServer.command`, every ADR's `governs:` truing-up for code/test paths) — commit `56cf5b3`, first-attempt pass · TASK-0055 — Relocate `specs/` and `adrs/` under `docs/`, rewire references, establish notes home (`docs/specs/**` incl. `project.md`, `docs/adrs/**`, new `docs/notes/README.md`, ADR `governs:` doc-path truing-up) — commit `92263d9`, first-attempt pass.
- **Tests:** 592 unit (Vitest, 26 files) + 191 UI (Playwright, 27 files) + 25 integration (Vitest, 7 files, live network) — all passing, **zero skipped**, after both relocations. The suites discovered and ran from the relocated `src/tests/{unit,ui,int}/**`, the Playwright `webServer` booted `node src/server/srv.js` serving the relocated client + `index.html`, and the integration tier resolved its `../../server`/`../../client` offsets — proving the move is behavior-preserving end-to-end. Integration ran within the documented live-network anti-flakiness tolerances (`strm.test.js` ≥1-of-5 sampled streams; `remux.test.js` ≥1 remux; one transient single-connection Xtream-portal contention on a first run cleared on re-run, confirmed not a relocation regression). No new integration command (this run adds no external connectivity). Verified green locally at review: `npx vitest run` → 592 passed.
- **Demo:** exempt — `No demo — pure structural relocation, no user-facing change`. The `### Demo` section of PR #27 carries that exact note (CORE_FLOW.md §3 scope cutoff: a pure relocation with no user-interactable behavior change is demo-exempt). The non-deterministic `test-results/*.webm|*.png` byte-regenerations produced by the UI run were deliberately left **unstaged** (out of scope, mirroring both task validations).
- **Rules earned:** none (no terminal failures).
- **Artifacts:** relocated under `src/` — `src/index.html`, `src/client/**`, `src/server/**`, `src/tests/{unit,ui,int}/**`; relocated under `docs/` — `docs/specs/**` (incl. `project.md`), `docs/adrs/**` (all 26 ADRs + `TEMPLATE.md`), new `docs/notes/README.md`; rewired at root — `package.json`, `vitest.config.js`, `vitest.int.config.js`, `playwright.config.js`; `docs/adrs/ADR-0026-relocate-src-docs-layout.md`, `tasks/TASK-005{4,5}-*.md`; `README.md` (run command `node src/server/srv.js`, `` `adrs/` `` → `` `docs/adrs/` ``); `BACKLOG.md` (consumed "Reorganize repo into docs/ and src/ top-level folders" entry removed). Traceability: every `governs:` path across all 27 ADR files resolves to a tracked file at the branch tip — code/test entries point at `src/…`, config/package entries stay at root, doc entries at `docs/…`; ADR-0026's directory entries (incl. `docs/notes/` → `docs/notes/README.md`) all resolve; deleted ADR-0018 keeps `governs: []` and appears in `src/` only as negative-regression test comments, never as governed code; root `specs/` and `adrs/` directories are gone.
- **Notes:** branch `ai/e16-docs-src-restructure`, PR **#27**, cut **fresh from `main`** (E15 merged at `f6c4853`) — its merge-base is the current `main` tip, so PR #27 has **no product-branch stacking dependency**. **Cross-PR merge-together dependency (CRITICAL):** PR #27 relocates product files + product references only; the companion harness PR **#28** (`harness/repoint-docs-src-paths`, by coreflow-agent) repoints the harness's own references to the new layout (`CLAUDE.md`, `CORE_FLOW.md`, `.claude/agents/**`, `.claude/skills/**`, `.claude/settings.json`, `.github/workflows/**`). The two PRs are **interdependent and MUST merge together** (or #28 first), or #28's forward-referenced paths break on `main`. This build branch is intentionally harness-inconsistent (e.g. on this branch `CLAUDE.md`'s directory map still says `specs/`/`adrs/`) — that is #28's job to reconcile, not a defect in #27. Confirmed at final review: the build branch's diff vs `main` touches **no** harness-owned file (`.claude/**`, `CLAUDE.md`, `CORE_FLOW.md`, `.github/**`) — those are PR #28's exclusively. validate-agent wrote both per-task Test Results blocks into PR #27; the repo is private. No open items remain.

---

## #15 — 2026-06-13 — Make HLS/TS a single contextual, clickable format chip instead of always-present no-op buttons

- **Prompt:** The HLS and TS controls in the top bar were always present and did nothing on click — replace them with one chip that is added on top contextually (only while a channel plays) and actually does something when clicked. (Consumed the parked BACKLOG idea "HLS/TS format chips that appear on top instead of dead nav buttons"; that entry was removed from `BACKLOG.md` on finalization.)
- **Outcome:** Shipped. Both tasks completed and validated on the first attempt each. A UI behaviour fix-plus-rework of the existing content-head surface only — no new streaming backend or format support. The two always-present `#chip-hls`/`#chip-ts` pills (and the `.fmt-chips` container) are fully gone from source; in their place a single `#fmt-chip` `<button>` appears only when an engine is resolved for a playing channel, labelled for that engine (HLS/TS, with a remuxed `.ts` surfacing HLS per ADR-0012), and a click toggles an inline `#fmt-detail` "Playing via …" line (`aria-expanded` mirrored) — purely presentational, never touching the state-machine phase or running engine. ADR↔code traceability holds fully and the run carries **no open items**.
- **ADRs:** ADR-0025 (`adrs/ADR-0025-contextual-format-chip.md`, **accepted**) — replace the always-present HLS/TS pills with one contextual, clickable format chip. `rndChip(eng)` is rewritten to show the chip (label + accent `.active`) only on a resolved engine and to hide it + collapse its detail otherwise (wired into `rndPlayer`'s stop/idle/error paths so it never lingers); a new `onFmtChip` click handler toggles the inline detail. Engine resolution is unchanged — `client/play.js` still pushes the resolved engine through `updChip` → `rndChip` (ADR-0010/ADR-0012 untouched); the chip reads ADR-0024 spacing/sizing tokens (28px secondary control, `--r1` radius) and ADR-0019 colour tokens. No accepted ADR is contradicted, superseded, or deleted.
- **Tasks:** 2 done, 0 failed, 0 blocked. TASK-0052 — Contextual single format chip: markup + show/hide + functional click toggle + styling (`index.html` `#fmt-chip`/`#fmt-detail`, `client/ui.js` `rndChip`/`onFmtChip` + wiring, `client/app.css` `.fmt-chip`; unit + UI tests, and `tests/ui/chips.test.js` migrated to the single-chip contract in the same task — no staged migration) — commit `2ab53f3` · TASK-0053 — Demo recording of the contextual format chip (`tests/ui/fmtchip-demo.test.js` → `test-results/e15-format-chip-contextual-demo.webm`) — commit `55ef484`. Both first-attempt passes.
- **Tests:** 592 unit (Vitest, 26 files — incl. `tests/unit/fmtchip.test.js`: `rndChip` resolution/label/hide + `onFmtChip` toggle + `aria-expanded` + no-phase-mutation cases) + 191 UI (Playwright — `tests/ui/fmtchip.test.js`: no chip at idle, exactly one labelled chip on play, click toggles the inline detail, chip disappears on stop; `tests/ui/chips.test.js` single-chip contract; plus the recorded demo arc) + 25 integration (Vitest, live network — regression gate, green within the documented ≥1-of-5 sampled-stream tolerance) — all passing, zero skipped. No new integration command (this run adds no external connectivity). Verified green locally at review: `npx vitest run` → 592 passed, `npx playwright test` → 191 passed.
- **Demo:** `test-results/e15-format-chip-contextual-demo.webm` (committed on the run branch), referenced from PR #26's `### Demo` as a clickable file-viewer link (private repo). Recorded arc: boot (fresh load, no chip at idle) → prepare (connect demo mode, play a channel so an engine resolves) → interact (the contextual chip appears on top labelled HLS; a click reveals the "Playing via hls.js" detail, a second click hides it; stopping playback hides the chip, proving contextual presence) → revert runtime state (in-app teardown back to idle) → stop.
- **Rules earned:** none (no terminal failures).
- **Artifacts:** `index.html` (single `#fmt-chip` button + `#fmt-detail` span replacing the `.fmt-chips` pill cluster), `client/ui.js` (`rndChip` rewrite + `onFmtChip` + `EL.fchp`/`EL.fdtl` wiring), `client/app.css` (`.fmt-chip` secondary-control style + hidden/detail affordance, `.fmt-chips` rule removed), `adrs/ADR-0025-contextual-format-chip.md`, `tasks/TASK-005{2,3}-*.md`, `tests/unit/fmtchip.test.js`, `tests/ui/{fmtchip,fmtchip-demo}.test.js`, `tests/ui/chips.test.js` (single-chip contract), `test-results/e15-format-chip-contextual-demo.webm`, `README.md` (Dual-engine-player bullet now describes the single contextual chip), `BACKLOG.md` (consumed "HLS/TS format chips that appear on top instead of dead nav buttons" entry removed; the deferred "Reorganize repo into harness/ and src/" entry retained). Traceability: all six of ADR-0025's `governs:` paths resolve to tracked files at the branch tip — `index.html` (`<!-- ADR: ADR-0025 -->`), `client/ui.js` (`ADR-0025` on its top ADR line), `client/app.css` (`/* ADR: ADR-0025 … */`), and the three test files (`tests/unit/fmtchip.test.js`, `tests/ui/fmtchip.test.js`, `tests/ui/fmtchip-demo.test.js`) each carry their `ADR-0025` reference; the `client/app.css` rewrite preserved the ADR-0024 (and all other) reference comments on its top line.
- **Notes:** branch `ai/e15-format-chip-contextual`, PR #26, stacks on the unmerged E14 → E13 → harness lineage; PR #26's diff against `main` therefore carries those commits and depends on them merging first (or together). validate-agent wrote both per-task Test Results blocks into PR #26; the repo is private, so the demo is referenced as a clickable blob-viewer link. **Traceability (confirmed clean at final review, single pass):** all six ADR-0025 `governs:` paths exist and carry their reference; no `governs:` path was over- or under-declared (unlike E14, which needed two extra rounds); a review scan of every **accepted** ADR confirmed all `governs:` paths resolve to tracked files and that no accepted ADR lost its traceability through the `client/app.css` rewrite; the one **deleted** ADR (ADR-0018) is referenced only by negative regression comments in `tests/ui/sidebar-filter.test.js` (asserting its affordances stay removed), not as governed code. `git ls-files test-results/` lists all tracked artifacts incl. `e15-format-chip-contextual-demo.webm`, so every PR #26 Test Results / `### Demo` link resolves at the branch tip. No open items remain.

---

## #14 — 2026-06-13 — Spacing & sizing unification pass: a single 4px-grid token contract (ADR-0024)

- **Prompt:** Spacing & sizing unification pass for the IPTV player UI — replace all eyeballed/hardcoded paddings, gaps, sizes, and radii with a single 4px-grid token contract so every block edge and control baseline aligns. (Consumed the parked BACKLOG idea of the same title; that entry was removed from `BACKLOG.md` on finalization.)
- **Outcome:** Shipped (product behaviour); all 5 tasks completed and validated on the first attempt each. A CSS-only restyle of the existing surfaces (sidebar, content header, player wrapper, channel grid/cards, footer) — no new screens, features, or behavioural change; column edges now line up on shared gutters and controls in a row share one height and baseline. ADR↔code traceability holds fully and the run carries **no open items**: the earlier `index.html` `governs:` discrepancy was RESOLVED on remediation (commit `a9d4513`), and all ten of ADR-0024's `governs:` paths (`client/app.css` + nine geometry test files) resolve to tracked files at the branch tip, each carrying its `ADR-0024` reference.
- **ADRs:** ADR-0024 (`adrs/ADR-0024-spacing-sizing-token-contract.md`, **accepted**) — a single 4px-grid spacing/sizing/radius token contract declared once on `:root` in `client/app.css` (`--s1..--s6` = 4/8/12/16/20/24px; `--gut` 24px content gutter / `--sgut` 16px sidebar gutter; `--ctl` 36px the one control height / `--hd` 56px header height; `--r1` 6px control radius / `--r2` 8px card+player radius). The geometry layer is orthogonal to ADR-0019's colour/font tokens (reused unchanged) and is **not** repeated in the `:root[data-theme="light"]` block, so it is theme-agnostic. Ten rewiring rules applied: per-column gutters, both 56px headers on one bottom line, primary 36px / secondary 28px controls (with a 28×28 star hit box), two radii + 4px badges, content vertical rhythm, sidebar category flex column, footer flex-end one-baseline form, mono `tabular-nums` numerals, and one global `:focus-visible` ring replacing every per-component focus rule. No accepted ADR is contradicted, superseded, or deleted; the two stated off-grid exceptions (badge min-width 26px / padding `3px 5px`, sidebar list gap 2px) are preserved verbatim as deliberate exceptions, not regressions.
- **Tasks:** 5 done, 0 failed, 0 blocked. TASK-0047 — declare the token contract on `:root` (`client/app.css`, commit `3a84fa5`) · TASK-0048 — column gutters + both 56px headers on one line (`client/app.css`, commit `dc21207`; CSS-only — no `index.html` change, and `index.html` is correctly **not** in ADR-0024's `governs:` after the remediation audit, see Notes) · TASK-0049 — content vertical rhythm, two-radii system, tabular numbers (`client/app.css`, commit `415f179`) · TASK-0050 — control heights, sidebar category list, footer baseline, one global focus ring (`client/app.css`, commit `a4b7a94`) · TASK-0051 — single end-to-end demo recording of the unified-grid surfaces (`tests/ui/grid-align-demo.test.js` → `test-results/e14-grid-align-demo.webm`, commit `6934be4`). All first-attempt passes.
- **Tests:** 582 unit (Vitest, 25 files — incl. `tests/unit/spacing.test.js` token-declaration + theme-agnostic + colour/font-token-still-present cases, `tests/unit/gutters.test.js` gutter/header source assertions, `tests/unit/rhythm.test.js` rhythm/radii/tabular-num cases, `tests/unit/controls.test.js` control-height/focus-ring cases) + 181 UI (Playwright — computed-geometry proofs: content-column blocks share one left x, sidebar blocks share one left x, both headers 56px on one bottom-border line, mobile gutter narrows to 16px at the 760px breakpoint, footer input + submit share 36px height and one baseline, theme-agnostic geometry, plus the 9-step recorded demo arc) + 25 integration (Vitest, live network — regression gate, green within the documented ≥1-of-5 sampled-stream tolerance) — all passing, zero skipped. No new integration command (this run adds no external connectivity).
- **Demo:** `test-results/e14-grid-align-demo.webm` (committed on the run branch), referenced from PR #25's `### Demo` as a clickable file-viewer link (private repo). Recorded arc: boot (fresh load via the canonical Playwright webServer; both 56px headers already on one bottom line) → prepare (connect demo mode, 31 cards) → interact (sidebar 16px gutter + 36px category rows + category select; both 56px headers on one continuous bottom line; content-column 24px gutter shared across `.content-head`/`.player-card`/`.ch-section`, footer padding asserted separately; channel select exercising the player wrapper; footer input + submit both 36px on one baseline; theme toggle to light proving theme-agnostic geometry) → revert runtime state (restore dark theme, stop playback, clear search, return to All Channels) → stop.
- **Rules earned:** none (no terminal failures).
- **Artifacts:** `client/app.css` (token contract on `:root` + every restyled rule rewired to read tokens), `specs/spacing-sizing.md` (new feature spec) + `specs/project.md` feature-spec list entry, `adrs/ADR-0024-spacing-sizing-token-contract.md`, `tasks/TASK-004{7,8,9}-*.md` + `tasks/TASK-005{0,1}-*.md`, `tests/unit/{spacing,gutters,rhythm,controls}.test.js`, `tests/ui/{spacing,gutters,rhythm,controls,grid-align-demo}.test.js`, `test-results/{e14-grid-align-demo.webm,task-0048-gutters.png,task-0049-grid-cards.png,task-0050-footer-baseline.png}`, `BACKLOG.md` (consumed "Spacing & sizing unification pass (4px-grid token contract)" entry removed). Traceability: `client/app.css` carries `ADR: ADR-0024` on its existing ADR comment line; the geometry test files carry their `ADR-0024` reference.
- **Notes:** branch `ai/e14-spacing-sizing-tokens`, PR #25, stacks on the unmerged E13 branch (PR #24) and the harness branch beneath it; PR #25's diff against `main` therefore carries those commits and depends on them merging first (or together). The E13 test-artifact-wipe issue stayed fixed — `git ls-files test-results/` lists all 13 tracked artifacts (incl. both `e13-`/`e14-` demos), so every PR #25 Test Results / `### Demo` link resolves at the branch tip. validate-agent wrote a per-task Test Results block into PR #25; the repo is private, so the demo and screenshots are referenced as clickable blob-viewer links. **Traceability (confirmed clean at final review):** Two `governs:` bookkeeping discrepancies were found and both are now RESOLVED. (1) The stale planned path `tests/ui/grid-align.test.js` flagged at first review was fixed by commit `dc9eceb` ("true up ADR-0024 governs"). (2) The second discrepancy — `index.html` wrongly listed under ADR-0024's `governs:` for a **CSS-only** restyle — was fixed by the remediation audit (commit `a9d4513`, "audit ADR-0024 governs (drop index.html — CSS-only, untraced)"), which dropped `index.html` from `governs:`, corrected the ADR prose, and re-ran all suites green (582 unit / 181 UI / 25 integration). ADR-0024 now governs exactly ten paths — `client/app.css` plus the nine geometry test files (`tests/unit/{spacing,gutters,rhythm,controls}.test.js`, `tests/ui/{spacing,gutters,rhythm,controls,grid-align-demo}.test.js`) — every one of which exists at the branch tip and carries its `ADR-0024` reference; `index.html` carries no `ADR-0024` comment and is correctly absent from `governs:`. A final review audit of all accepted ADRs confirmed every `governs:` path resolves to a tracked file and no accepted ADR lost its traceability through the `client/app.css` rewrite. No open items remain.

---

## #13 — 2026-06-13 — Clearer, more helpful empty / no-signal UI states (ADR-0021/0022/0023)

- **Prompt:** Improve the empty state / "no signal" UI — make it clearer and more helpful when there is no signal or no content to display. (Consumed the parked BACKLOG idea of the same title; that entry was removed from `BACKLOG.md` on finalization.)
- **Outcome:** Shipped. All 4 tasks completed and validated on the first attempt each. Every empty/no-signal moment now tells the user *what happened* and *what to do next*, reusing existing navigation affordances — no new feature, no redesign.
- **ADRs:** ADR-0021 (`adrs/ADR-0021-empty-state-model.md`, **accepted**) — one pure `client/empty.js` resolver (`window.IptvEmpty`) owns all message logic: `resolveContent({total,shown,flt,srch,favs})` → channel-grid `EmptyState` (or `null` when non-empty) and `resolveSignal({phase,cur})` → player no-signal `EmptyState`, returning the structured `{icon,title,body,action?}` shape with `action.kind` from the fixed set `clear-search`/`view-all`/`retry`/`connect`; no DOM, no globals beyond the export · ADR-0022 (`adrs/ADR-0022-no-content-grid-placeholders.md`, **accepted**) — `rndGrid` renders the resolved grid `EmptyState` inside `.ch-empty` (icon + title + body + optional action button), `role="status"`, `aria-hidden` icon, HTML-escaped query, action buttons wired to the existing clear-search / view-all handlers (no new navigation) · ADR-0023 (`adrs/ADR-0023-no-signal-player-placeholders.md`, **accepted**) — `rndPlayer` renders both player no-output states from `resolveSignal`: idle keeps the antenna + "NO SIGNAL" title with adaptive guidance (pick a channel vs connect a source, `connect` action only in the no-session/`INIT` case, `role="status"`); stream-error shows a warning icon, the constant headline "This channel won't play", a friendly explanation, a **Retry** button, and the raw engine token preserved only as a dimmed `.sig-detail` line (`role="alert"`). Retry re-attempts the current channel via a small `goPlay` re-play entry point in `client/play.js` — no new state-machine phase. All three are first-in-force this evolution; no prior ADR superseded or deleted.
- **Tasks:** 4 done, 0 failed, 0 blocked. TASK-0043 — pure empty-state resolver module (`client/empty.js`, commit `7446122`) · TASK-0044 — contextual no-content grid placeholders + actions (`client/ui.js` `rndGrid`, `client/app.css` `.ch-empty`, including the `mkEmpty → mkEmptyBox` shared-window-scope collision fix caught by the real-DOM UI tier, commit `494f9da`) · TASK-0045 — guided no-signal idle + stream-error placeholders with Retry (`client/ui.js` `rndPlayer`, `client/play.js` `goPlay`, `client/app.css`, `index.html` idle/err hooks now empty `role`-bearing containers, commit `95357f8`) · TASK-0046 — single end-to-end demo recording of the improved states (`tests/ui/empty-demo.test.js` → `test-results/e13-empty-state-demo.webm`, commit `72086f5`). All first-attempt passes.
- **Tests:** 484 unit (Vitest, 21 files — incl. `tests/unit/empty.test.js` resolver purity + priority cases, `tests/unit/grid.test.js` contextual-placeholder render + role/aria + action-button cases, `tests/unit/signal.test.js` idle/error render cases, and `goPlay` cases in `tests/unit/play.test.js`) + 153 UI (Playwright — grid placeholder + Clear-search/Browse-all restore, idle/error player placeholders + focusable Connect/Retry + role=status/role=alert, and the 9-step recorded demo arc) + 25 integration (Vitest, live network — regression gate, green within the documented ≥1-of-5 sampled-stream tolerance) — all passing, zero skipped. No new integration command (this run adds no external connectivity). Verified green locally at review: `npx vitest run` → 484 passed.
- **Demo:** `test-results/e13-empty-state-demo.webm` (723 KB, committed on the run branch), referenced from PR #24's `### Demo` as a clickable file-viewer link (private repo). Recorded arc: boot (fresh load via `node server/srv.js`) → prepare (connect demo mode, 31 channels) → interact (every improved state: no-match grid + Clear search, empty favourites + Browse all, empty category, idle "NO SIGNAL" guidance, stream-error + Retry) → revert runtime state (in-app teardown back to idle/All Channels) → stop.
- **Rules earned:** none (no terminal failures).
- **Artifacts:** `client/empty.js` (new), `client/ui.js` (`rndGrid`/`rndPlayer`), `client/play.js` (`goPlay`), `client/app.css` (`.ch-empty` + signal placeholder styles), `index.html` (empty `#player-idle`/`#player-err` `role` containers + `empty.js` script before `ui.js`), `specs/empty-states.md` (new), `specs/project.md` (feature-spec list entry), `adrs/ADR-002{1,2,3}-*.md`, `tasks/TASK-004{3,4,5,6}-*.md`, `tests/unit/{empty,signal,grid,play}.test.js`, `tests/ui/{grid,player,empty-demo}.test.js`, `test-results/e13-empty-state-demo.webm`, `README.md` (channel-grid + dual-engine-player feature bullets note the contextual placeholders and guided no-signal/Retry UX), `BACKLOG.md` (consumed "Clearer, more helpful empty / no-signal state UI" entry removed). Traceability: `client/empty.js` carries `ADR: ADR-0021`; `client/ui.js`, `client/app.css`, `client/play.js`, `index.html` carry their `ADR-0022`/`ADR-0023` references on their existing ADR comment lines.
- **Notes:** branch `ai/e13-empty-state-ui`, PR #24, stacks on the unmerged harness branch (HEAD lineage includes `5b22675` "harness: self-publish-and-demo"); PR #24's diff against `main` therefore carries those harness commits and depends on the harness PR merging first (or together). validate-agent wrote a per-task Test Results block into PR #24; the repo is private, so the demo and screenshots are referenced as clickable blob-viewer links. **PR-evidence discrepancy — RESOLVED by remediation (commit `286b9f7`):** the original TASK-0046 commit (`72086f5`) let Playwright's default `outputDir` (`test-results/`) wipe the committed `.png` screenshots, so the UI Test Results blocks for TASK-0043/0044/0045 briefly referenced blob URLs that did not resolve at the branch tip. The remediation moved `outputDir` to the gitignored `.playwright-out/` (`playwright.config.js`, `.gitignore`), re-ran all suites green (484 unit / 153 UI / 25 integration), and re-committed `test-results/` with all 7 referenced artifacts tracked (`task-0044-empty-no-match.png`, `task-0045-player-idle.png`, `task-0045-player-error.png`, `theme-light.png`, `theme-dark.png`, `task-0040-plain-sidebar.png`, `e13-empty-state-demo.webm`). `git ls-files test-results/` now lists all seven, so every PR #24 Test Results / `### Demo` link resolves at the branch tip; this never affected code or test correctness.

---

## #12 — 2026-06-13 — Fix M3U sidebar categories: first-level segment of group-title (ADR-0020)

- **Prompt:** The M3U sidebar shows messy per-group category buttons (`Classic;Comedy;Public;Series`, `Classic;Series`, …) — fix the M3U category parsing. (Mid-run scope correction: the run first wrongly made the M3U path return **no** categories / a flat sidebar; the human corrected the requirement to **keep** M3U categories but derive each from only the **first-level** segment of `group-title` — split on `;`, take `[0]`, trim, dedupe — so the `Classic;*` variants collapse to a single `Classic`. The spec/ADR were revised in place and TASK-0042 re-implemented forward; history was not rewritten.)
- **Outcome:** Shipped. The single task completed and validated on the first attempt of the corrected (forward) implementation. M3U / community-preset sources now show clean, real first-level categories (one `Classic` button, no `Classic;…` near-duplicates); Xtream (`get_live_categories`) and demo categories are unchanged.
- **ADRs:** ADR-0020 (`adrs/ADR-0020-m3u-first-level-categories.md`, **accepted**) — M3U / playlist category is the first-level segment of `group-title` (the text before the first `;`, trimmed, deduped, first-seen order); `getM3uCats` is **retained** (not removed), `parsM3u`/`loadM3u` return those first-level categories, and `mkM3uCh` sets both `grp` and `cat` to the first-level segment (fallback `"Other"`) so the grid filter `ch.cat === id` matches the sidebar button id. **Refines** ADR-0005's `group-title → categories` parse step (value used is now the first segment, not the raw string); ADR-0005 stays **accepted** with a corrected pointer to ADR-0020. ADR-0009 (Xtream categories), the demo category set, ADR-0017 (sort), ADR-0019 (theme), and the account/preset decisions are untouched. (ADR-0020 was earlier drafted around the wrong "empty categories" reading; it was rewritten in place to the first-level decision and renamed accordingly — the earlier empty/flat-sidebar behavior is fully replaced, with no leftover "M3U has no categories" text in code, spec, or tests.)
- **Tasks:** 1 done, 0 failed, 0 blocked. TASK-0042 — M3U category = first-level segment of `group-title`: added the pure `firstSeg(g)` helper (`String(g).split(';')[0].trim()`, `"Other"` fallback), re-introduced deduplicated first-seen `getM3uCats`, wired it through `parsM3u`/`loadM3u`, and set `mkM3uCh` `grp`/`cat` to the first-level segment (`client/api.js`). First-attempt pass of the forward re-implementation (commit `c958abf`; the earlier wrong `categories: []` commit `d7be888` was superseded forward on the same branch, not reverted).
- **Tests:** 441 unit (Vitest) + 136 UI (Playwright) + 25 integration (Vitest, live network) — all passing, zero skipped. Unit (`tests/unit/api.test.js`) asserts the `Classic;*` variants collapse to one `Classic`, no category id/name contains `;`, and each channel `cat === grp === firstSeg`; UI (`tests/ui/m3u.test.js`) asserts the rendered sidebar shows the deduplicated first-level buttons (no `;`-bearing labels) with Xtream/demo sidebars unchanged; integration (`tests/int/m3u.test.js`) asserts the live iptv-org playlist yields non-empty, deduplicated, first-level-only categories (no `;`) while > 100 channels parse and conform to `CH_DEF`. No new integration command (this fix touches no new external connectivity).
- **Rules earned:** none (no terminal failures).
- **Artifacts:** `client/api.js` (`firstSeg`/`getM3uCats`/`mkM3uCh`/`parsM3u`/`loadM3u`), `tests/unit/api.test.js`, `tests/ui/m3u.test.js`, `tests/int/m3u.test.js`, `specs/` M3U parse-step + ADR-0005 pointer revision, `adrs/ADR-0020-m3u-first-level-categories.md` (rewritten + renamed), `tasks/TASK-0042-flatten-m3u-categories.md`, `README.md` (Category sidebar note → first-level group-title). Traceability: `client/api.js` and the three test files carry `ADR: ADR-0020`.
- **Notes:** this branch (`ai/e12-flatten-m3u-sidebar`, PR #16) stacks on the unmerged E11 branch (`ai/e11-remove-genre-browsing`, PR #14), so PR #16's diff against `main` includes E11's commits and depends on PR #14 merging first (or together). validate-agent wrote the per-task Test Results block into PR #16; the repo is private, so UI screenshots are referenced as clickable blob-viewer links.

---

## #11 — 2026-06-13 — Remove the uniform category/genre browsing layer (ADR-0018)

- **Prompt:** Remove the derived/uniform category-genre browsing — it did not work reliably across all streaming networks; revert to the plain per-engine category list.
- **Outcome:** Shipped (feature removal). All 3 tasks completed and validated, first attempt each; the sidebar reverts to the pre-E9 plain per-engine category list.
- **ADRs:** ADR-0018 (uniform category/genre browsing — derived genres, filterable `#cat-filter` sidebar, alphabetical ordering, active-genre `#genre-chip`) marked **`status: deleted`** with `governs: []` and an evolution note + back-reference; the ADR file is **retained as history** per CORE_FLOW §3 (ADR files are never removed). No new ADR created. ADR-0017 (channel sorting), ADR-0019 (theme), ADR-0013/0014 (accounts), ADR-0016 (presets), ADR-0009/0005/0001 (category data model + restored plain browsing) are all **preserved and untouched**.
- **Tasks:** 3 done, 0 failed, 0 blocked. TASK-0039 remove the ADR-0018 genre helpers (`getCats`/`catName`/`cmpCat`/alphabetical ordering) and restore source-order categories (`client/srch.js`, `client/ui.js` `rndSide`, `client/cfg.js` `S.catFltMin` removed) · TASK-0040 remove the filterable sidebar (`#cat-filter`/`#cat-list`) + active-genre chip (`#genre-chip`, `rndHead` reverted to the plain channel-name render) and restore the plain sidebar (`index.html`, `client/app.css`, `client/ui.js`, `client/main.js`) · TASK-0041 delete the dedicated ADR-0018 test files (`tests/unit/genre.test.js`, `tests/ui/genre.test.js`) — surviving sidebar/category assertions moved to `tests/ui/sidebar-filter.test.js` as a negative regression gate proving `#cat-filter`/`#cat-list`/`#genre-chip` stay absent — plus a cleanup audit confirming zero `ADR: ADR-0018` traceability tags remain across `client/ server/ tests/`. All first-attempt passes. The demo-fixture cat-id casing fix from TASK-0036 (`ch.cat === cat.id` for demo channels) was **kept and re-attributed to ADR-0009** — a normalization-consistency correctness fix the surviving plain category browsing depends on, not an ADR-0018 feature.
- **Tests:** 441 unit (Vitest) + 133 UI (Playwright, incl. the negative regression gate that the removed `#cat-filter`/`#cat-list`/`#genre-chip` affordances resolve to count 0, plain category-click grid filtering, and that sort/theme/accounts/presets/search/favourites remain intact) + 25 integration (Vitest, live network — green at TASK-0041 validation, within the documented ≥1-of-5 stream and ≥1 remux tolerances) — all passing, **zero skipped**. Counts drop from E10's 462/137 because the dedicated ADR-0018 unit and UI test files were removed; no surviving assertion was weakened. No new integration command (this removal touches no external connectivity).
- **Rules earned:** none (no terminal failures).
- **Artifacts removed/changed:** `client/srch.js` (`getCats` removed), `client/ui.js` (`rndSide`/`rndHead` reverted, no filter/chip), `client/cfg.js` (`S.catFltMin` removed), `client/main.js`, `index.html` (`#cat-filter`/`#cat-list`/`#genre-chip` markup removed), `client/app.css` (filter/chip styles removed), `specs/iptv-player.md` (§5a active-genre-chip + §5b uniform-genre-browsing/filter text removed — category list documented as source-order, no genre filter, no alphabetical re-ordering), `adrs/ADR-0018-category-genre-browsing.md` (status → `deleted`, `governs: []`, evolution note), `tasks/TASK-003{9,40,41}-*.md`. Deleted test files: `tests/unit/genre.test.js`, `tests/ui/genre.test.js` (surviving assertions → `tests/ui/sidebar-filter.test.js`).
- **Notes:** this branch (`ai/e11-remove-genre-browsing`, PR #14) was cut fresh from `main` (5a897a9, which already has E7–E10 merged), so it has **no stacking dependency** on an unmerged branch — PR #14's diff against `main` is this removal alone. validate-agent wrote a Test Results block per task into PR #14; the repo is private, so UI screenshots are referenced as clickable blob-viewer links.

---

## #10 — 2026-06-12 — Light/dark theme switching via a sun/moon toggle in the top-right

- **Prompt:** Add light/dark theme switching with a sun-vs-moon toggle in the top-right corner.
- **Outcome:** Shipped. Both tasks completed and validated, first attempt each.
- **ADRs:** ADR-0019 (light/dark theme via a `data-theme` attribute on `<html>` driving a `:root[data-theme="light"]` token-override rule that re-defines the same eight colour tokens — `--bg`/`--sur`/`--sur2`/`--ln`/`--tx`/`--dim`/`--acc`/`--live` — so every token-reading rule recolours for free; dark is the default and stays on `:root` (no flash, `index.html` ships with **no** `data-theme`, per R-0001); the choice persists under a new `iptv_theme` key (`S.themeKey`, ADR-0003 pattern) via `st.js` `loadTheme()`/`saveTheme()`; theme is presentational chrome — **not** an `ST` phase field (ADR-0001 / CONVENTIONS §6 unaffected); a keyboard-focusable sun↔moon `#theme-toggle` (`role="switch"` + `aria-checked`) mounts in `.content-head` immediately before the account button `#acct-btn` (ADR-0014), applied/persisted by `rndTheme`/`onTheme` in `ui.js` and restored on load in `main.js`).
- **Tasks:** 2 done, 0 failed, 0 blocked. TASK-0037 theme key + persistence helpers + light-theme token rule (`client/cfg.js` `S.themeKey`, `client/st.js` `loadTheme`/`saveTheme` + the uniquely-named `THMS`/`THM_DEF` top-level bindings to avoid a shared-window-scope collision, `client/app.css` `:root[data-theme="light"]`) · TASK-0038 sun/moon toggle markup + CSS + handlers + init (`index.html`, `client/app.css`, `client/ui.js` `EL.thm`/`rndTheme`/`onTheme`, `client/main.js`). Both first-attempt passes.
- **Tests:** 462 unit (Vitest) + 137 UI (Playwright, incl. baseline-has-no-data-theme R-0001 checks, the light token override recolouring computed bg/surface/text, the toggle visible top-right coexisting with the account button without overlap, click + keyboard activation switching theme, sun/moon state reflecting the active theme, and persistence across reload) — all passing. Integration tier (25, live network) not re-run this evolution — the theme is client-only (localStorage + CSS, no external connectivity); last green at E9 TASK-0036.
- **Rules earned:** none (no terminal failures).
- **Artifacts:** `client/cfg.js`, `client/st.js`, `client/ui.js`, `client/main.js`, `client/app.css`, `index.html`, `specs/project.md` (theme-switchable palette note + `specs/theme.md` in the feature-spec list), `specs/theme.md` (new), `tests/unit/{theme,themetoggle,persist}.test.js`, `tests/ui/{theme,themetoggle}.test.js`, `test-results/theme-{dark,light}.png` (UI screenshots), `adrs/ADR-0019-theme-toggle.md`, `tasks/TASK-003{7,8}-*.md`.
- **Notes:** this branch (`ai/e10-light-dark-theme-toggle`, PR #13) was cut from `harness/fix-private-repo-screenshots` (PR #12) → E9 (PR #11) → harness Test Results (PR #10) → E8 (PR #9) → E7 (PR #8), so PR #13's diff against `main` includes all those commits and depends on PRs #8, #9, #10, #11, and #12 merging first. The validate-agent wrote a collapsible Test Results block per task into PR #13; because the repo is private, the UI screenshots are referenced as clickable blob-viewer links (an inline raw embed would 404 under GitHub's anonymous image proxy) — the screenshot mechanism was corrected on PR #12 this session.

---

## #9 — 2026-06-12 — Channel sorting control + filterable category/genre browsing + active-genre chip

- **Prompt:** Add channel sorting and channel categories/genres browsing.
- **Outcome:** Shipped. All 4 tasks completed and validated, first attempt each.
- **ADRs:** ADR-0017 (user-selectable channel sort — four stable tokens `num-asc`/`name-asc`/`name-desc`/`fav-first` with default `num-asc`, applied as the final ordering step inside the pure `getChs(chs, q, flt, favs, sort)`; a single exported `IptvSrch.SORTS` `{id,label}[]` is the source of truth for the UI control and the comparator; `ST.sort` flat field + sole writer `setSort`, persisted globally as `iptv_sort` (ADR-0003 pattern), restored only for one of the four known tokens; a labelled native `<select id="ch-sort">` in the `.ch-bar` toolbar) · ADR-0018 (uniform category/genre browsing — keeps the already-uniform Xtream/M3U category data model unchanged, improves browsing only: a pure `getCats(cats, q)` helper filters categories by case-insensitive substring and returns them name-ascending; `rndSide` renders categories alphabetically with "All Channels"/"Favourites" pinned and adds a `#cat-filter` input above `S.catFltMin` (default 12) categories with a view-local, non-persisted query reset on connect/switch/disconnect; an active-genre chip in the content-head shows the playing channel's `grp` via `rndHead`).
- **Tasks:** 4 done, 0 failed, 0 blocked. TASK-0033 sort tokens + comparator + persistence (`client/srch.js`, `client/st.js`, `client/cfg.js`, all five `getChs` call sites migrated in one task to avoid a half-migrated runtime; the st.js persistence guard list is named `SRTS` to avoid a shared-window-scope collision with srch.js's exported `SORTS` — caught by the full UI suite, invisible to the isolated unit harnesses) · TASK-0034 pure `getCats(cats, q)` genre filter + alphabetical ordering (`client/srch.js`) · TASK-0035 sort control in the channel-grid toolbar (`index.html`, `client/ui.js`, `client/app.css`, `client/main.js`) · TASK-0036 filterable sidebar genre list + active-genre chip (`client/ui.js`, `client/cfg.js`, `client/main.js`, `index.html`, `client/app.css`; also fixed a pre-existing demo-fixture cat-id casing bug in `client/api.js` so `ch.cat === cat.id` for demo channels — consistent with ADR-0009's normalization, no decision content changed — making demo category browsing yield real channels). All first-attempt passes.
- **Tests:** 431 unit (Vitest) + 122 UI (Playwright, incl. the sort control reordering/persistence across reload, the genre-filter input around the threshold with All/Favourites pinned, and the active-genre chip from the selected channel's `grp`) + 25 integration (Vitest, live network — green at TASK-0036 validation, 1 sampled iptv-org stream endpoint down within the ≥1-of-5 tolerance, not a regression) — all passing. No new integration command this run (sort + genre browsing are derived from already-fetched data; no new connectivity).
- **Rules earned:** none (no terminal failures).
- **Artifacts:** `client/srch.js`, `client/st.js`, `client/cfg.js`, `client/ui.js`, `client/main.js`, `client/api.js` (demo cat-id casing fix), `index.html`, `client/app.css`, `specs/iptv-player.md` (§5a genre chip, §5c/§5d sort control, §9 `iptv_sort`), `tests/unit/{srch,sort,genre,persist}.test.js` (+ harness trues-up in foot/side/acctui), `tests/ui/{sortctl,genre}.test.js`, `adrs/ADR-001{7,8}-*.md`, `tasks/TASK-003{3,4,5,6}-*.md`.
- **Notes:** this branch was cut from `harness/pr-test-results-blocks` (PR #10), which stacks on the E8 branch (PR #9) → E7 branch (PR #8), so PR #11's diff against `main` includes the E7/E8/harness commits and depends on PRs #8, #9, and #10 merging first. The validate-agent ran its updated definition and wrote a collapsible Test Results block per task into PR #11 (with inline committed screenshots for the UI tiers of TASK-0035/0036).

---

## #8 — 2026-06-12 — Community preset accounts: selectable iptv-org playlists in the account panel

- **Prompt:** Add iptv-org as a default account in an "other accounts" list users can select from — a community-provided playlist list.
- **Outcome:** Shipped. Both tasks completed and validated, first attempt each.
- **ADRs:** ADR-0015 (community preset accounts — a static, curated, all-M3U `S.psts` catalog of five iptv-org playlists under the durable `https://iptv-org.github.io/iptv/` path, a `Pst` typedef, and a pure `getPst()` helper; selecting a preset connects on the explicit M3U path (ADR-0008) and becomes an ordinary saved `Acct` (ADR-0013) deduped by `url+user+m3u` — no distinct account kind, no second store; introduces the CONVENTIONS `preset → pst` / `presets → psts` tokens) · ADR-0016 (community presets section in the account panel — a default, always-present `#acct-psts` block below the saved-accounts list and above "Add account", read-only catalog rows with no remove control, `is-active` marking the connected preset, one-click M3U connect reusing the `runSwitch`/`onSwOk` machinery; extends ADR-0014 without changing it).
- **Tasks:** 2 done, 0 failed, 0 blocked. TASK-0031 community presets data: `S.psts` curated list, `Pst` typedef, `getPst()` helper (`client/cfg.js`, `client/st.js`, `tests/unit/preset.test.js`) · TASK-0032 render the community presets section + wire selection to an M3U connect (`index.html`, `client/ui.js`, `client/app.css`, `tests/unit/acctui.test.js`, `tests/ui/acct.test.js`). Both first-attempt passes.
- **Tests:** 384 unit (Vitest) + 108 UI (Playwright, incl. the Community playlists section present with zero saved accounts, no-remove affordance, panel DOM order, and the active-preset-click no-op) — all passing. Integration tier (25, live network) last run green at TASK-0032 validation; no new integration command this run — every preset URL is an iptv-org M3U already covered end-to-end by the existing live tier (`tests/int/m3u.test.js` exercises `index.m3u`), and the unchanged M3U engine + proxy path adds no endpoints.
- **Rules earned:** none (no terminal failures).
- **Artifacts:** `client/cfg.js`, `client/st.js`, `client/ui.js`, `client/app.css`, `index.html`, `CONVENTIONS.md` (§1 `preset`/`presets` tokens), `specs/iptv-player.md`, `tests/unit/{preset,acctui}.test.js`, `tests/ui/acct.test.js`, `adrs/ADR-001{5,6}-*.md`, `tasks/TASK-003{1,2}-*.md`.
- **Notes:** this run branched from the E7 branch HEAD (`ai/e7-account-switcher-panel`, PR #8), not from `main`, because the feature extends E7's account panel which is not yet merged; PR #9 therefore includes E7's commits and depends on PR #8 merging first (or together).

---

## #7 — 2026-06-12 — Multiple saved accounts: top-right nav button + right slide-in account panel

- **Prompt:** Add an account feature that displays the connected account and server URL, allows switching and adding accounts, surfaced as a top-right nav button that opens a right-side panel.
- **Outcome:** Shipped. All 4 tasks completed and validated.
- **ADRs:** ADR-0013 (multiple saved accounts — an `iptv_accts` list of `Acct` records + an `iptv_act` active-pointer replace the single `iptv_creds` record; pure `st.js` helpers `loadAccts`/`getAct`/`addAcct` (dedupe by `url+user+m3u`)/`rmAcct`/`mkAcct`/`saveAccts`/`saveAct`/`clearAct`, plus a one-time read-time migration of any legacy `iptv_creds`; supersedes ADR-0003's credential-persistence portion only — `iptv_sel`/`iptv_favs` carry forward) · ADR-0014 (account nav button at the right of `.content-head` + a right slide-in `#acct-panel` aside with scrim, open/close driven solely by an `is-open` CSS class, no new ST phase).
- **Tasks:** 4 done, 0 failed, 0 blocked. TASK-0027 account store: `Acct` type, `S` keys, `st.js` helpers + legacy migration (`client/cfg.js`, `client/st.js`, `tests/unit/{acct,persist,cfg}.test.js`) — required 2 attempts: attempt 1 dropped the legacy `iptv_creds` runtime path before the rewiring task, breaking 6 UI persist tests; attempt 2 retained a compatibility shim that TASK-0029 then removed · TASK-0028 nav button + right slide-in panel shell, CSS, open/close (`index.html`, `client/app.css`, `client/ui.js`, `tests/unit/acctui.test.js`, `tests/ui/acct.test.js`) · TASK-0029 wire connect/reconnect/switch/disconnect through the account store, removing the legacy shim (`client/{cfg,st,main,ui}.js`) · TASK-0030 render panel contents: connected block, list, switch/remove/add (`client/{ui,main,app.css}.js/css`). TASK-0028/0029/0030 first-attempt passes.
- **Tests:** 363 unit (Vitest) + 104 UI (Playwright, incl. the account panel open/close, switch, add, and remove flows) — all passing. Integration tier (25, live network) last run green at TASK-0029 validation; no integration command added this run (reuses the existing `IptvApi.connect`/proxy path).
- **Rules earned:** none (no terminal failures).
- **Artifacts:** `client/cfg.js`, `client/st.js`, `client/main.js`, `client/ui.js`, `client/app.css`, `index.html`, `specs/iptv-player.md` (§13 Accounts + §5a/§9/§12 updates), `tests/unit/{acct,acctui,persist,cfg,foot,side}.test.js`, `tests/ui/{acct,persist}.test.js`, `adrs/ADR-001{3,4}-*.md`, `adrs/ADR-0003-persistence.md` (supersede note), `tasks/TASK-002{7,8,9}-*.md`, `tasks/TASK-0030-*.md`.

---

## #6 — 2026-06-12 — TS→HLS remux fallback for MSE-less devices (iOS Safari)

- **Prompt:** Fix "MPEG-TS not supported" when opening streams after login (MSE-less devices, e.g. iOS Safari).
- **Outcome:** Shipped. Both tasks completed and validated, first attempt each.
- **ADRs:** ADR-0012 (server-side TS→HLS live remux fallback — ffmpeg stream copy via the `ffmpeg-static` npm package, `/api/hls` endpoint with per-source sessions, idle reaping, and segment serving; client falls back to the remuxed HLS through the existing hls.js/native-HLS path when MSE is unavailable; extends ADR-0010/ADR-0011, both stay accepted).
- **Tasks:** 2 done, 0 failed, 0 blocked. TASK-0025 server remux endpoint (`server/hls.js`, mounted from `server/rtr.js`: SSRF-gated `GET /api/hls?url=`, session reuse, `+delete_segments` sliding window, 502 on ffmpeg failure/startup timeout, traversal-safe segment route, idle-reap teardown) · TASK-0026 client fallback (`client/play.js`: `hasTs()` false → `runHls(getRmx(url))` with the raw stream URL, HLS chip reflects the engine in use, "MPEG-TS not supported" only on double failure; MSE-capable behavior byte-identical to E5).
- **Tests:** 297 unit (Vitest) + 89 UI (Playwright, incl. MSE-less stubbed fallback) + 25 integration (Vitest, live network — incl. live remux of a real portal channel to a fetchable `#EXTM3U` playlist with valid TS segments, and the client-built fallback URL proven against the in-process server) — all passing.
- **Rules earned:** none.
- **Artifacts:** `server/hls.js` (new), `server/rtr.js`, `client/play.js`, `package.json` (`ffmpeg-static`), `specs/iptv-player.md`, `specs/integration-testing.md`, `tests/unit/{hls,play}.test.js`, `tests/ui/{fallback,chips}.test.js`, `tests/int/{remux,e2e}.test.js`, `adrs/ADR-0012-server-hls-remux-fallback.md`, `tasks/TASK-002{5,6}-*.md`.

---

## #5 — 2026-06-12 — Xtream connect + stream playback against a personal portal

- **Prompt:** iptv can't connect to the personal Xtream test portal `http://mymax.top:8080` — make it connect and play a stream.
- **Outcome:** Shipped. All 4 tasks completed and validated.
- **ADRs:** ADR-0009 (Xtream path normalizes portal objects into the canonical `Ch` schema, auth check + `allowed_output_formats`-driven stream-URL construction) · ADR-0010 (MPEG-TS playback via mpegts.js 1.7.x CDN, engine selected by stream-URL extension, HLS/TS chips become live engine indicators; refines ADR-0004) · ADR-0011 (proxy follows validated redirects up to 5 hops, pipes long-lived streams without size/timeout caps, aborts upstream on client disconnect; extends ADR-0002).
- **Tasks:** 4 done, 0 failed, 0 blocked. TASK-0021 Xtream normalization + stream-URL building (`client/api.js`, `tests/int/xtream.test.js`) · TASK-0022 proxy redirect-following + stream piping (`server/rtr.js`, `tests/{unit,int}/redir.test.js`) · TASK-0023 dual-engine player mpegts.js/hls.js (`client/play.js`, `client/ui.js`, `index.html`, `tests/unit/play.test.js`, `tests/ui/chips.test.js`) · TASK-0024 live end-to-end proof against mymax.top (`tests/int/e2e.test.js`, `tests/ui/live.test.js`). All first-attempt passes.
- **Tests:** 269 unit (Vitest) + 83 UI (Playwright) + 20 integration (Vitest, live network — including live connect, category/channel listing, and TS-byte playback through the redirect-following proxy) — all passing.
- **Rules earned:** none.
- **Artifacts:** `client/api.js`, `client/play.js`, `client/ui.js`, `index.html`, `server/rtr.js`, `specs/iptv-player.md` (§5a, §8, §10), `tests/unit/{play,side,redir}.test.js`, `tests/ui/{chips,live}.test.js`, `tests/int/{xtream,redir,e2e}.test.js`, `adrs/ADR-00{09,10,11}-*.md`, `tasks/TASK-002{1,2,3,4}-*.md`.
- **Notes:** entry #4 below was recorded retroactively during this review — the E4 run merged (PR #5) without its review-phase CHANGELOG entry, leaving a #3 → #5 gap that this evolution's review closed.

---

## #4 — 2026-06-11 — Explicit login-mode choice (recorded retroactively at E5 review)

- **Prompt:** Explicit login-mode choice — the user picks Xtream vs M3U; URL auto-detection removed.
- **Outcome:** Shipped (merged to `main` as PR #5). Recorded retroactively: the E4 run's review phase never appended this entry; the facts below come solely from the repository's files and git history.
- **ADRs:** ADR-0008 (explicit login-mode choice, supersedes ADR-0005's auto-detection).
- **Tasks:** 4 done, 0 failed, 0 blocked. TASK-0017 engine explicit `opts.m3u` connect routing · TASK-0018 footer login-mode selector UI · TASK-0019 persist login mode in `iptv_creds` + reconnect with stored mode · TASK-0020 remove the `isM3u` auto-detect heuristic.
- **Rules earned:** none recorded (no E4 failure records exist in `failures/`).
- **Artifacts:** `client/api.js`, `client/ui.js`, `index.html`, `adrs/ADR-0008-explicit-login-mode.md`, `tasks/TASK-00{17,18,19,20}-*.md` (per ADR-0008 `governs:` and the task files).

---

## #3 — 2026-06-11 — Live integration tests for the iptv-org M3U engine path

- **Prompt:** Write integration tests validating the engine can connect to and load `https://iptv-org.github.io/iptv/index.m3u` streams over the live network.
- **Outcome:** Shipped. All 3 tasks completed and validated.
- **ADRs:** ADR-0006 (integration-test tier — Vitest separate config `vitest.int.config.js`, in-process Express server on ephemeral ports, live network required, no mocks/auto-skip) · ADR-0007 (live iptv-org M3U validation — full engine-path load assertions plus sampled stream reachability with the ≥ 1-of-5 anti-flakiness criterion).
- **Tasks:** 3 done, 0 failed, 0 blocked. TASK-0014 harness scaffolding (`vitest.int.config.js`, `test:int` npm script, `tests/int/proxy.test.js` live proxy connectivity, 2 tests) · TASK-0015 live engine connect+load (`tests/int/m3u.test.js`, fetch shim + `IptvApi.connect` against the live playlist, 5 tests) · TASK-0016 sampled stream reachability (`tests/int/strm.test.js`, deterministic 5-stream sample, 15 s per-stream timeout, 3 tests). All first-attempt passes.
- **Tests:** 206 unit (Vitest) + 66 UI (Playwright) + 10 integration (Vitest, live network) — all passing.
- **Rules earned:** none.
- **Artifacts:** `vitest.int.config.js` (new), `tests/int/{proxy,m3u,strm}.test.js` (new), `package.json` (`test:int` script), `specs/project.md` (integration canonical command `npx vitest run --config vitest.int.config.js`), `specs/integration-testing.md` (new), `adrs/ADR-000{6,7}-*.md`, `tasks/TASK-001{4,5,6}-*.md`.
- **Notes:** mid-run, `origin/main` (PR #2 merge) was merged into the run branch at the human's request (merge commit 8d975cb).

---

## #2 — 2026-06-11 — M3U playlist URL support

- **Prompt:** Add M3U playlist URL support — users can paste any `.m3u` / `.m3u8` URL (e.g. `https://iptv-org.github.io/iptv/index.m3u`) and the app connects, browses, and plays channels with the same UX as Xtream portals.
- **Outcome:** Shipped. All 4 tasks completed and validated.
- **ADRs:** ADR-0005 (M3U playlist support — detection, parse, CORS proxy reuse, footer UI adaptation).
- **Tasks:** 4 done, 0 failed, 0 blocked. TASK-0001 hot-fix: `res.headersSent` guard in `server/rtr.js` preventing ERR_HTTP_HEADERS_SENT crash · TASK-0011 `isM3u()` + `parsM3u()` pure functions · TASK-0012 `loadM3u()` fetch+parse integration + `connect()` routing · TASK-0013 footer UI adaptation (hide username/password for M3U URLs). TASK-0013 required 3 attempts — initial unit tests incorrectly asserted `setAttribute('required')` on inputs that never carry `required` in the HTML; corrected to `removeAttribute('required')` in both M3U and non-M3U branches, earning R-0001.
- **Tests:** 206 unit (Vitest) + 66 UI (Playwright) — all passing.
- **Rules earned:** R-0001 (FAIL-0001): Before writing unit tests that assert DOM attribute mutations, check the baseline HTML to confirm which attributes are actually present on the element.
- **Artifacts:** `server/rtr.js` (headersSent guard), `client/api.js` (isM3u, parsM3u, loadM3u, connect routing), `client/ui.js` (updM3u, onUrlInput), `client/app.css` (`.is-m3u` hide rules), `index.html` (field-user/field-pass classes), `tests/unit/api.test.js` (extended), `tests/unit/rtr.test.js` (extended), `tests/unit/m3u-ui.test.js` (new), `tests/ui/m3u.test.js` (new), `tests/ui/m3u-ui.test.js` (new), `adrs/ADR-0005-m3u-playlist-support.md`, `tasks/TASK-0001-*.md`, `tasks/TASK-001{1,2,3}-*.md`.

---

## #1 — 2026-06-11 — IPTV Player broadcast console (initial build)

- **Prompt:** Build an IPTV Player broadcast console — single-page web app connecting to Xtream-compatible portals with category browsing, channel search, favourites, HLS playback via hls.js, and localStorage persistence.
- **Outcome:** Shipped. All 10 tasks completed and validated.
- **ADRs:** ADR-0001 (client stack — vanilla JS + state machine), ADR-0002 (server stack — Node.js + Express, CORS proxy), ADR-0003 (persistence — localStorage), ADR-0004 (video playback — hls.js).
- **Tasks:** 10 done, 0 failed, 0 blocked. TASK-0001 scaffolding · TASK-0002 API client · TASK-0003 state machine · TASK-0004 CSS · TASK-0005 sidebar + search · TASK-0006 channel grid · TASK-0007 player (hls.js) · TASK-0008 footer · TASK-0009 entry point · TASK-0010 localStorage persistence.
- **Tests:** 178 unit (Vitest) + 60 UI (Playwright) — all passing.
- **Rules earned:** none.
- **Artifacts:** `index.html`, `client/{api,app.css,cfg,main,nav,play,srch,st,ui}.js`, `server/{cfg,rtr,srv}.js`, `specs/iptv-player.md`, `adrs/ADR-000{1,2,3,4}-*.md`, `tasks/TASK-000{1..10}-*.md`, `tests/unit/{api,cfg,foot,grid,persist,play,rtr,srch,st}.test.js`, `tests/ui/{css,foot,grid,persist,placeholder,player,sidebar,smoke}.test.js`.

---

## #0 — 2026-06-11 — Harness bootstrapped

- **Prompt:** Install the orchestration harness (orchestrator + spec /
  implement / validate / review agents, failure→rule learning loop).
- **Outcome:** Shipped. Harness installed; no product exists yet.
- **ADRs:** none (harness setup predates product decisions).
- **Tasks:** none.
- **Rules earned:** none.
- **Artifacts:** `CORE_FLOW.md`, `CLAUDE.md`, `.claude/agents/{spec,implement,validate,review}-agent.md`, templates in `adrs/`, `tasks/`, `failures/`, seed `specs/project.md`, this file, `README.md`.
