# Copy to .env.secrets and fill in. .env.secrets is gitignored — never commit real values.
# Required by .github/workflows/validate-ai-instructions.yml (set as a GitHub repo secret too)
ANTHROPIC_API_KEY=
