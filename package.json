{
  "name": "iptv-broadcast-console",
  "version": "0.1.0",
  "main": "src/server/srv.js",
  "scripts": {
    "start": "node src/server/srv.js",
    "test": "npx vitest run",
    "test:int": "vitest run --config vitest.int.config.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ffmpeg-static": "^5.3.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "vitest": "^1.6.0"
  },
  "allowScripts": {
    "ffmpeg-static@5.3.0": true
  }
}
